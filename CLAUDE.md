# Mossmark — Project Guide

## Project Goal

A top-down 2D RPG focused on **world and town development** rather than player character progression. The player moves through the world and holds a single **Attention** action on whatever is nearby — what happens is a property of the thing being attended to, not a menu choice. Sustained attention develops NPCs, buildings, and the town over time, revealing new specializations and access rather than presenting an upgrade menu. Tech is per-town and must be manually transferred between towns in later phases.

See [IDEAS.md](IDEAS.md) for the high-level vision and [FEATURES.md](FEATURES.md) for the current feature design and iteration roadmap.

> **Historical Note:** This is "Prototype 2" — a fresh Unity project started from scratch (see PROTOTYPE2.md's "Why a second prototype"). It is informed by Prototype 1 but does not carry over P1's architecture; P1 systems referenced in PROTOTYPE2.md (e.g. `PlayerMovement`, `DayCycleManager`, `Entity`/`UpgradePool`) are reference material from the old project, not code that exists here yet. (Iterations beyond P2's "30" are currently sourced in FEATURES.md)

---

## Design Values

These are the load-bearing principles of Mossmark. They take precedence over implementation convenience, feature completeness, and genre convention. When a new system or mechanic is in tension with one of these, the value wins unless there's an explicit documented decision to defer it.

**Tried, not chosen.** Outcomes emerge from sustained attention rather than menu selection. The player never picks what happens — only where they stand and how long they stay. Any interaction that presents a player-facing choice of outcome type is pulling against this. The dependency resolver produces a "needs" message when something is blocked; it does not ask the player to select from a list of options.

**Organic over deterministic.** Outcomes should feel like they emerged from the world, not like a counter hit a threshold. Where a system has a meaningful threshold, approach it probabilistically — a rising chance per tick, not a precise trigger. Use min/max ranges wherever the variance is felt. State that changes gradually should change continuously, not in discrete steps. Feedback describes conditions, not values: "the ground here is disturbed," not "tendedness: 0.24." Gates (binary `CanAttend()` checks) can be exact — they're legible and intentional. Outcomes should not be. *A known audit pass is pending to apply this to existing implementations.*

**The world was here before you and will outlast you.** The settlement is not rescued by the player's arrival. It existed before and will continue after. Development reveals what was latent — latent specializations in dilapidated buildings, latent knowledge in people — rather than installing new things from outside. NPCs are not quest-givers waiting for the player to activate them; they're people with their own trajectories that the player's attention can influence. Nothing about the world should feel like it was placed there as content for the player to consume.

**Differentiation, not advancement.** A settlement deep in ritual knowledge and a settlement deep in ironworking are not at different points on the same ladder — they're genuinely different places. Development gives a settlement character, not score. Resist any framing where one development path is objectively better than another, or where there's a canonical "complete" state. The design tension between enjoying development systems and discomfort with "progress ideology" is resolved by making outcomes discovered rather than selected — see "Tried, not chosen" above.

**No preaching.** The post-imperial Dark Ages setting is a mood and a context, not a thesis. The game does not argue that pre-modern societies were noble, that development is inherently good, or that the player is rescuing anyone. The people in this world are complicated; the history is complicated; the player's role is to tend, not to save. Avoid mechanics that frame the settlement as deficient without the player's help, or that reward the player for "civilizing" anything. 

**Felt, not read.** The world communicates through texture rather than UI. Knowledge-gated yield changes are noticed by returning to a familiar spot, not announced. Spot tendedness is described in overlay language, not displayed as a stat. Maintenance drift is a building that seems quieter, not a timer bar. When in doubt, delay the feedback and make it indirect — the player should feel like they discovered something, not that they received a notification.

**The Flame Sword feeling.** Some moments must feel like the world giving you something earned. A rare item found after patient attention to a dusk wilderness run, an NPC gift after a long-developed relationship, a POI that opens because someone has become who the place needed — these moments lose their weight if everything flattens into the same small grindy texture. Preserve room for larger payoff moments even as the moment-to-moment loop becomes granular.

---

## Environment

- **Unity version**: 6000.4.11f1 (Unity 6)
- **Render pipeline**: URP, 2D Renderer (`Assets/Settings/Renderer2D.asset`). The active scene has a Global Light 2D — 2D lights are available and may be used later for day/night ambience (the daylight/day cycle in PROTOTYPE2.md).
- **Physics**: 2D (`Rigidbody2D`, gravity = 0 for top-down movement)
- **Input**: new Input System (`com.unity.inputsystem`), via the project-wide actions asset `Assets/InputSystem_Actions.inputactions`. See "Input Actions" below.
- **UI system**: Unity UI Toolkit (`UIDocument`, `VisualTreeAsset`, `VisualElement`) — **not** uGUI/Canvas
- **Editor tooling**: `com.gamelovers.mcp-unity` is installed for MCP-based editor automation (not yet wired into this session's tools).

---

## Input Actions

`Assets/InputSystem_Actions.inputactions` is configured as the project-wide actions asset (Project Settings → Input System Package). Access it via `InputSystem.actions`, enabling the relevant map in `OnEnable`:

```csharp
var gameplay = InputSystem.actions.FindActionMap("Gameplay");
gameplay.Enable();
var move = gameplay.FindAction("Move");
```

- **`Gameplay` map**
  - `Move` — `Vector2`, WASD / arrow keys / gamepad left stick. Drives `PlayerController`.
  - `Attend` — `Button` with a `Hold` interaction (keyboard `E`, gamepad North button). Drives `AttentionInput` (see Key Design Decisions for how its `started`/`canceled` callbacks are used).
  - `Horizon` — plain `Button` (keyboard `Tab`). Toggles the Settlement Horizon panel (`HorizonUI`).
- **`UI` map** — retained from the default template for future menu surfaces (e.g. the settlement chest, the one menu-based interaction PROTOTYPE2.md keeps).

---

## Code Conventions

### Language and approach
- **C# only** — no visual scripting (no Bolt, no Unity Visual Scripting graphs)
- **Code-first**: all game logic lives in MonoBehaviours, plain C# classes, and ScriptableObjects
- **1st-party APIs first**: prefer Unity's built-in systems over 3rd-party plugins for gameplay code
- **No 3rd-party gameplay frameworks**

### Namespaces
All scripts live under the `Mossmark` root namespace, organized by system. This table reflects PROTOTYPE2.md's "Core Systems" section, generalized from Prototype 1:

| Namespace | Purpose |
|---|---|
| `Mossmark.Player` | Player controller, camera follow, input relay |
| `Mossmark.Visuals` | Shared grey-box visual primitives (e.g. `TriangleSpriteGenerator`) used across systems |
| `Mossmark.Attention` | Attention framework (zones, detector, manager, overlay UI) — generalizes P1's Interaction framework (`IInteractable` → `IAttendable`) |
| `Mossmark.Day` | Daylight/day-clock — phase tracking, ambient text, and the daylight HUD. Direct successor to P1's `DayCycleManager` |
| `Mossmark.Inventory` | Inventory manager, item database, item pickups, settlement chest |
| `Mossmark.Development` | Generic dependency/response resolver and developable entities (buildings, NPCs, POIs, town) — generalizes P1's `Entity`/`UpgradePool`/`TownEntity` |
| `Mossmark.World` | Region and town generation, place archetypes, and wilderness-spot attendables (e.g. `DevelopingWildernessSpotAttendable`, `PoiAttendable`) |

P1's `Mossmark.Quests` and `Mossmark.Combat` are **not** part of P2 — collection quests and discrete combat encounters were dropped per IDEAS.md's "Prototype 2" section. Add namespaces here as new systems land; keep this table in sync with what's actually implemented.

### Tooling language default
- **Default to C# for all project tooling** (Editor scripts, build helpers, data importers). Unity's `AssetDatabase`, `SerializedObject`, and `EditorUtility` APIs give direct, type-safe access to all game data without external dependencies. Only reach for another language when there is a concrete reason C# cannot do the job (e.g., a CI script that must run outside the Unity process and has no natural C# equivalent).
- Editor scripts live in `Assets/Editor/` and use `[MenuItem]` for discoverability inside the Editor. They are excluded from player builds automatically.

### Architecture rules
- **Event-driven**: systems communicate via `UnityEvent<T>` or C# `event Action<T>`, not direct method calls across system boundaries
- **Singleton managers**: one per system (e.g., `AttentionManager.Instance`), implemented as `MonoBehaviour` singletons with `Awake` guard
- **UI holds no game logic**: UI components only display state pushed from managers; they never write to game state directly
- **Interfaces for contracts**: use interfaces (e.g. `IAttendable`) for cross-system contracts so systems stay decoupled
- **ScriptableObjects for data**: item definitions, development stages, region/place-archetype data, loot tables — all as `ScriptableObject` assets stored under `Assets/Game/Data/`. Do **not** use `Assets/Game/Resources/` for new assets.
- **Procedural spawning — inactive-GO pattern**: when creating GameObjects at runtime, call `SetActive(false)` immediately after `new GameObject()`, add all components and call their `Initialize()` methods, then `SetActive(true)`. This ensures `Awake` fires with correct data already set on serialized fields. Components that participate in procedural spawning must expose a public `Initialize()` method.
- **Each iteration leaves prior systems functional**: never break existing systems when adding new ones

### Comments
Default to no comments. Only add a comment when the *why* is non-obvious (a hidden constraint, a Unity quirk, a workaround). Never describe what the code does — good naming handles that.

---

## Active Scenes

- **`Assets/Game/Scenes/Greybox.unity`** — the active prototype scene; all new work targets this scene
- **`Assets/Settings/Scenes/URP2DSceneTemplate.unity`** — Unity's URP 2D scene template (used when creating new scenes via the Editor); not part of gameplay

---

## System Overview (Prototype 2)

| System | Status | Key Types |
|---|---|---|
| Input + Movement | Complete | `PlayerController`, `CameraFollow` |
| Grey-box Visuals | Complete | `TriangleSpriteGenerator` |
| Attention Framework Core | Complete | `IAttendable`, `AttendableZone`, `AttendableDetector`, `AttentionInput`, `AttentionManager` |
| Attendable Overlay UI | Complete | `AttendableOverlayUI` |
| Generic Wilderness Spots | Complete | `GenericWildernessSpotAttendable`, `ItemYield` |
| Inventory + HUD | Complete | `ItemDefinition`, `InventoryManager`, `InventoryUI` |
| Daylight / Day Clock | Complete | `DayCycleManager`, `DayCycleAmbientTextData`, `DayCycleUI` |
| Bedroll + Day Transition | Complete | `BedrollAttendable`, `DayTransitionFadeUI`, `DayCycleManager.Rest()` |
| Tended-Style Spots | Complete | `TendedSpotAttendable` |
| Generic Dependency / Response Resolver | Complete | `IDependencyCondition`, `DevelopmentStage`, `DevelopmentTrack`, `DevelopableEntity`, `WorldState` |
| Continuous Attention Framework | Complete | `IAttendable.ContinueAttending`, `AttentionManager` tick loop |
| Buildings: Revival via Attention | Complete | `BuildingAttendable` |
| NPCs: Development + Specialization Draw | Complete | `NpcAttendable` |
| Building → NPC Demand Loop | Complete | `DeclaredSpecializationNeeds`, `SpecializationNeededCondition` |
| World Gen: Place Archetypes | Complete | `PlaceArchetype`, `RegionData`, `WorldGenerator` |
| POIs | Complete | `PoiAttendable`, `RealizedSpecializations`, `SpecializationRealizedCondition`, `ItemYieldRoller` |
| Wandering Things | Complete | `WanderingThingAttendable`, `WanderingThingSpawner` |
| Settlement Chest (Rebuilt) | Complete | `ChestAttendable`, `ChestUI` |
| Settlement Horizon UI | Complete | `HorizonUI` |
| World/Town Layout (Visual Polish) | Complete | `WorldLayoutGenerator`, `SquareSpriteGenerator` |
| NPC Post-Specialization Tracks | Complete | `WoundLoreModifier`, `WorldStateChanceModifier` (+ `DevelopableEntity.MarkStageAsApplied`, `OutcomeRequest.DaylightCostMultiplier`) |
| Wilderness Depth + Random Placement | Complete | `WildernessSpotDefinition`, `TendedSpotAttendable.Initialize()`, `WorldGenerator.FindValidPosition()` |
| Building Data Generalization (G1+G2) | Complete | `BuildingStageDef`, `BuildingAttendable` stages array, `PlaceArchetype` building block |
| NPC Post-Spec + Spot Tick Tuning (G3+G6) | Complete | `NpcPostSpecStageDef`, `PlaceArchetype.NpcPostSpecStages`, `WildernessSpotDefinition` tick intervals |
| Wandering Thing Definition (G4+G5) | Complete | `WanderingThingDefinition`, `WorldStateOddsModifier`, `WanderingThingSpawner` pool, `traveler.asset` |
| Code Quality Pass (G7+G8+G9) | Complete | `WildernessYieldAttendable` base class, `LandmarkAttendable`, `TendedSpotAttendable.harvestYields[]` |
| CSV / Data Pipeline | Complete | `Assets/Editor/ExportGameData.cs`, `Assets/Editor/ImportGameData.cs` |
| Numeric Tuning Pass (Iteration 26) | Complete | `place_archetypes.csv`, `wilderness_spots.csv`, `wandering_things.csv`, `Greybox.unity` NPC tick fields |
| Always-Something-Happens (Iteration 28.5) | Complete | `NpcAttendable` visit mechanic, `BuildingAttendable` flavor linger, `PlaceArchetype` exchange/restored-flavor data |
| Settlement Maintenance (Iteration 29) | Complete | `IMaintenanceConsumer`, `MaintenanceManager`, `DevelopableEntity.DriftProgress`, `BuildingAttendable`/`NpcAttendable` cold state, `PlaceArchetype` maintenance fields |
| Settlement Growth: New Arrivals (Iteration 30) | Complete | `ArrivalCondition`, `ArrivalAttendable`, `ArrivalSpawner`, `NpcAttendable.Initialize()`, `settlement_grew` WorldState flag |
| Passive Drift Pilot (Iteration 31) | Complete | `NpcAttendable.OnDayAdvanced()`, `WorldGenerator.GetArchetypeSpot()`, `WildernessYieldAttendable.Tendedness` |
| State-Change Feedback Pass (Iteration 32) | Complete | `EntityFeedback`, `CircleSpriteGenerator`, `DevelopableEntity.OnProgressMade`, `WildernessYieldAttendable.OnProgressMade`, `TendedSpotAttendable.OnProgressMade`, `NpcAttendable.OnPassiveDriftAccrued`, `PlayerController.HandleAttentionRock` |
| Progress Cost Tuning Pass (Iteration 33.1) | Complete | `archetype_bog.asset`, `archetype_old_road.asset`, `archetype_sacred_grove.asset`, `place_archetypes.csv` post-spec stage costs |
| Cross-Pursuit Influence (Iteration 34) | Complete | `NpcPostSpecStageDef.passiveDriftSourceArchetypeId`, `KnowledgeYieldEntry.requiredSpecializationId`, `BuildingStageDef.worldStateFlag`, `BuildingAttendable.HandleDeveloped()` |
| Item Properties: Folk-Phrase Vocabulary (Iteration 35) | Complete | `PropertyDefinition`, `PropertyRegistry` (static), `PropertyKnowledge`, `ItemDefinition.PropertyIds`, `WorldContext.IsPropertyKnown()`, `InventoryUI` property display |
| Conversion Station: Crude Working Surface (Iteration 36) | Complete | `ConversionDef` SO (item-keyed + property-keyed inputs, `TryMatch()`), `WorkshopAttendable` (single restoration stage via flat_stones), `WorkshopUI` (3 slots + Work, property discovery on failure), 3 recipes, 3 new item assets, `EntityFeedback.TriggerPop()` |
| Property-Phrased Wants: Loop Closure (Iteration 37) | Complete | `PropertyAvailableCondition` (carry-only property gate, authored `wantDescription` shown in overlay), Bog Keeper `bog_keeper_drainage` stage gates on `turns_water` (Mistletoe or Clay satisfy it), matched item consumed from pack at stage completion. *(The `requiredPropertyId` field this shipped with was superseded by Iteration 38's condition list.)* |
| Relational Data Architecture Migration (Iteration 38) | Complete | `ConditionCsvImporter` (CSV → `managedReferenceValue`), `CsvUtil`, `YieldTable`, `NpcStageDef` + `NpcStagePool` (replaces `NpcPostSpecStageDef`), `BuildingStageDef` (now a SO) + `BuildingStagePool`, `PlaceArchetype.Spots` (spot list replaces inline spot block), `WildernessSpotDefinition.spotId`, `WorldGenerator.GetSpot()`, `ItemYieldRoller` weighted rare pools, `npcMaintenanceMaterial`. Schema: [DATA_SCHEMA.md](DATA_SCHEMA.md) |
| Multi-Station Property Discovery (Iteration 39) | Complete | `BuildingStageDef.stationName`/`.biasPropertyIds`, `IWorkStation`, `BuildingAttendable` station branch (replaces `WorkshopAttendable`, deleted), `WorkshopUI` bias filtering, `workshop_pool`, `bog_fen_shrine` stage, `recipe_sealing_daub`, `recipe_warding_charm` |
| Station Availability Decoupled from Final Stage (Iteration 40) | Complete | `BuildingAttendable.AppliedStationStage` (most-recently-applied-stage lookup, replaces `FinalStage`/`IsStationOpen`), `.CanAttend()` (`\|\| IsStationCapable`), `.OnAttentionComplete()` (maintenance → develop-if-possible → open station → linger) |
| Outcome Cost Resolution: Duration + Daylight (Iteration 41) | Complete | `AttentionOutcomeKind`, `BuildingAttendable.PredictOutcomeKind()`/`.BuildOutcomeRequest()`, `DriftColdDaylightModifier`, `OutcomeRequest.DurationMultiplier` |
| Site Clustering + Progressive Reveal: Bog Pilot (Iteration 42) | Complete | `WorldGenerator.SpawnArchetypeSites()`/`.FindValidPositionNear()`, `PlaceArchetype.PoiDormantByDefault`/`.PoiRevealWorldStateFlag` *(superseded by Iteration 45's `PoiStartingTier`/`PoiRevealCondition`)*, `HintFlavorEntry`, `NpcAttendable.OnDayAdvanced()` `_started` flag |
| Wilderness Spot Development Stages + Exhaustion/Standing Split: Fen Bog Pilot (Iteration 43) | Complete | `SpotStageDef`, `SpotStagePool`, `SustainedGoodAttentionCondition`, `IGoodAttentionTracker`, `DevelopingWildernessSpotAttendable`, `ITendednessSource`, `DevelopableEntity.RaiseProgressMade()` |
| Exhaustion Cost Tiering: Felt, Not Read (Iteration 43.1) | Complete | `ExhaustionCostModifier`, `DevelopingWildernessSpotAttendable.BuildOutcomeRequest()` |
| Exhaustion/Standing Rollout: All Generic Wilderness Spots (Iteration 44) | Complete | `spot_stages.csv`, `ImportGameData.ImportSpotStages()`, `ExportGameData.ExportSpotStages()`, `wilderness_spots.csv` `spotStagePool` column, `GenericWildernessSpotAttendable` retired |
| POI Three-Tier Reveal: Hidden/VisibleInert/Interactable (Iteration 45) | Complete | `PoiTier`, `PlaceArchetype.PoiStartingTier`/`.PoiRevealCondition`/`.PoiUnlockCondition`, `PoiAttendable` collider-gated tiering, `SpotStageDef.WorldStateFlag` |
| POI Three-Tier Rollout: Old Road, Sacred Grove, Old Quarry (Iteration 46) | Complete | `archetype_old_road.asset`/`archetype_sacred_grove.asset`/`archetype_quarry.asset` `poiStartingTier`/`poiRevealCondition`/`poiUnlockCondition`, `old_road_familiar.asset`/`old_quarry_familiar.asset`/`stone_outcrop_familiar.asset` `worldStateFlag` |

Full iteration plan is in [FEATURES.md](FEATURES.md). Update this table as each iteration lands.

---

## Key Design Decisions

Active implementation constraints carried forward from prior iterations. Full iteration-by-iteration history is in [CLAUDE_ARCHIVE.md](CLAUDE_ARCHIVE.md).
**Patterns that remain active constraints (full history in CLAUDE_ARCHIVE.md):**

- **`RequiresDaylight` / `ContinueAttending` are read *after* `OnAttentionComplete()`**, not before. This means `OnAttentionComplete()` sets a latch (`lastAttentionMadeProgress`, `lastAttentionWasVisit`) and those flags drive what the `AttentionManager` does next. Never read them before the call or derive them from pre-call state.
- **Inactive-GO spawning pattern**: `SetActive(false)` → add components + call `Initialize()` → `SetActive(true)`. `Awake()` fires with correct data already in place. All procedurally spawned entities follow this.
- **Asset references (`UnityEngine.Object` fields) cannot be set via MCP-Unity `update_component`**. Set them by hand-editing the scene `.unity` YAML, then calling `load_scene` to pick up the change before `save_scene`. This applies to `ItemDefinition`, `PlaceArchetype`, `WildernessSpotDefinition`, and similar SO references on spawned components. **This limitation is specific to MCP-Unity's generic tools, not the engine** — custom Editor scripts set object references (and even `[SerializeReference]` polymorphic values, via `SerializedProperty.managedReferenceValue`) freely; the CSV importer (Iteration 38) is now the primary path for wiring references on *asset* data, leaving hand-YAML only for *scene* components.
- **`DayCycleManager.DayAdvanced` event is the hook for all per-rest state changes.** Subscribe in `Start()`, unsubscribe in `OnDestroy()`. `Start()` is used (not `Awake()`) because `DayCycleManager.Instance` is guaranteed available after all `Awake()`s have run.
- **`WorldContext` / `IOutcomeModifier` / `OutcomeRequest`** is the approved pattern for ambient world-state influencing outcomes. Do not hardcode world-state checks inside `OnAttentionComplete()` — add a modifier. `OutcomeRequest` currently carries `ChanceMultiplier` and `DaylightCostMultiplier`; add new dimensions only when a modifier concretely needs one.
- **`KnowledgeYieldModifier` was deliberately not created as an `IOutcomeModifier`** — it would have required a cross-namespace dependency (`Mossmark.Development` → `Mossmark.World`). Knowledge yield injection lives in `WildernessYieldAttendable.BuildKnowledgeInjectedYields()` instead. Follow this precedent: keep modifier logic in the World namespace when the data it touches is World-scoped.
- **`using System;` is avoided in files that use `UnityEngine.Random`** — it creates an ambiguous `Random` reference. Use `System.Array.Copy` (explicit), not `Array.Copy`.
- **`tendedness` does not hard-reset to `0.5f` on rest** — it drifts continuously. `+0.03f` if attended that day, `-0.08f` if not. Depletion (`<0.3`) is reachable after ~3 unattended rests from baseline; peak (`>0.7`) after ~6–7 attended rests. Tuning baselines only.
- **`TendedSpotAttendable` is excluded from tendedness** — it has its own `restsToHarvest` rest-cycle rhythm that conflicts with the continuous drift model.
- **NPC visit mechanic (Iteration 28.5)**: fully-developed NPCs (`drawnSpecializationId != null && GetNextStage() == null`) bypass `ResolveAttention()` and call `RunVisitInteraction()` instead. `lastAttentionWasVisit = true` makes `RequiresDaylight => true` (costs 1 daylight) and `ContinueAttending => false` (one-shot). Universal specs get flavor only; archetype specs roll `npcExchangeChance (0.2)` for a gift. Exchange flavor text is indexed to the gifted item — text and item can never mismatch.
- **Building linger mechanic (Iteration 28.5)**: fully-developed buildings (`GetNextStage() == null`) deliver a random flavor line at no daylight cost. `lastAttentionWasVisit` sets `RequiresDaylight => false`, `ContinueAttending => false`.
- **CSV pipeline (Iteration 25, restructured in Iteration 38) uses C# Editor scripts**, not Python. `ExportGameData.cs` / `ImportGameData.cs` / `CsvUtil.cs` / `ConditionCsvImporter.cs` under `Assets/Editor/`. `ReadCsv()` skips `#`-prefixed comment lines. Item references stored as `ItemDefinition.DisplayName` strings, resolved to assets at import time via a pre-loaded dictionary. Unbounded lists (stages, conditions, yield-table entries) are one-row-per-entry relational files joined by id — the old slot-numbered column prefixes (`stage1_*`, `bStage1_*`, `spotKnowledge1_*`) are gone; only genuinely per-owner short lists (knowledge yields on spots, wandering-thing modifiers) keep dynamic per-run-maximum columns.
- **Tuned baseline values (Iteration 26)**: archetype spot tick intervals 2.0–2.5s; generic spot tick intervals 1.5–2.0s; NPC tick intervals 1.5–2.0s; Building Stage 1 `progressCost` 4 (8 items total at 2/tick); `maxDaylight` 24; `NpcAttendable.progressCost` 8; rare drop chance 6–8% base ×1.5 at dawn/dusk. See `place_archetypes.csv` comment block for full reasoning.
- **`IMaintenanceConsumer` interface (Iteration 29)** (`Mossmark.Development`) exposes `DriftThreshold`, `MaintenanceMaterial`, `MaintenanceCostPerReset`. `MaintenanceManager` (`[DefaultExecutionOrder(500)]`) subscribes to `DayCycleManager.DayAdvanced`, then in `OnDayAdvanced()` runs two ordered passes: (1) increment all developed entities' drift, (2) silently consume from chest for any `IMaintenanceConsumer` with `DriftProgress > 0` and sufficient stock. Order matters — all entities increment before any chest resolution, so one chest stock can cover multiple entities in a single rest.
- **`DevelopableEntity` drift fields (Iteration 29)**: `DriftProgress { get; private set; }`, `IncrementDrift()` (no-op pre-development), `ResetDrift()` (no-op if already 0, calls `OnDriftReset()`), `protected virtual OnDriftChanged()` / `OnDriftReset()` (empty base, overridden by `NpcAttendable`). `GetDriftOverlayDescription(displayName, threshold, coldFlavor)` returns `displayName` below 60% threshold, `"{displayName} — needs tending"` in the warning band, and `coldFlavor` (fallback `"{displayName} — seems neglected"`) at or above threshold.
- **`BuildingAttendable` maintenance priority order (Iteration 29)**: (1) direct maintenance — if `DriftProgress > 0` and carrying `maintenanceCostPerReset` of `maintenanceMaterial`, consume from pack, `ResetDrift()`, post notification, return; (2) linger (fully developed); (3) development. Cold buildings add an extra `SpendDaylight(1)` on productive development ticks (total 2 daylight per tick). `lastAttentionWasMaintenance` guards `RequiresDaylight => false` (maintenance costs no daylight) and `ContinueAttending => false` (one-shot). `maintenanceMaterial` is set from `stages[0].material` in `Awake()` — revival material = maintenance material.
- **`NpcAttendable` maintenance (Iteration 29)**: archetype NPCs implement `IMaintenanceConsumer` with `maintenanceMaterial` set from `archetype.CommonYields[0].Item` in `HandleDeveloped()`. Universal-spec NPCs get `null maintenanceMaterial` — their visit IS the maintenance path (`RunVisitInteraction()` always calls `ResetDrift()` when `DriftProgress > 0`). `setWorldStateFlags: HashSet<string>` tracks all flags set by post-spec stages; `OnDriftChanged()` suspends them all at the cold threshold; `OnDriftReset()` restores them. Priority order mirrors `BuildingAttendable`: (1) direct maintenance; (2) visit/linger (always resets drift if drifted); (3) development.
- **`PlaceArchetype` maintenance fields (Iteration 29)**: `buildingColdFlavor`, `buildingMaintenanceCost` (default 2), `npcColdFlavor`, `npcMaintenanceCost` (default 1), under a `[Header("Maintenance")]` block. CSV pipeline updated with these 4 columns. Cold flavor values start empty — authored per-archetype in a future content pass.
- **`HorizonUI` drift (Iteration 29)**: `Refresh()` now reads `attendable.GetOverlayDescription()` for the entity name column instead of `entity.DisplayName`, so drift suffix appears in the Settlement Horizon panel automatically.
- **`ArrivalCondition` (`Mossmark.World`, Iteration 30)** is a `[Serializable]` `IDependencyCondition` whose scope is the world rather than any entity. Checks `string[] requiredFlags` (all must be true via `WorldContext.GetFlag()`) and optionally `int minimumDevelopedEntities` (count of `DevelopableEntity` instances with `CurrentStageIndex >= 0`). `GetNeedsDescription()` returns `""` — arrivals are silent. The `entity` parameter to `IsSatisfied` is ignored, following the pattern from `PoiAttendable`'s gate.
- **`ArrivalAttendable` (`Mossmark.World`, Iteration 30)**: thin `MonoBehaviour, IAttendable`, not a `DevelopableEntity`. `AttentionDuration => 2f`, `RequiresDaylight => false`, `ContinueAttending => !promoted && warnessProgress < warnessThreshold`. `OnAttentionComplete()` increments `warnessProgress` and fires `onPromote(gameObject)` on threshold crossing. `CanAttend() => !promoted` (inert once promoted, before Unity destroys the GO).
- **`ArrivalSpawner` (`Mossmark.World`, Iteration 30)**: holds `ArrivalTrigger[]` (each entry a `[Serializable]` inner class with condition, color, name, NPC params). Subscribes to `DayCycleManager.DayAdvanced`. Per-trigger `[NonSerialized] bool fired` and `[NonSerialized] ArrivalAttendable spawnedArrival` prevent duplicate spawning — `fired` is set on promotion and never clears (one arrival per trigger per session). `OnArrivalPromoted()` creates the new `NpcAttendable` via inactive-GO pattern, calls `NpcAttendable.Initialize()`, sets `WorldState.SetFlag("settlement_grew", true)`, posts `"The settlement grows."`, then `Destroy(arrivalGo)` — safe because Unity queues destruction until end-of-frame, so `AttentionManager` reads `ContinueAttending => false` cleanly first.
- **`NpcAttendable.Initialize()` (Iteration 30)** is a new public method (`genericName`, `progressCost = 8`, `minTickInterval = 1.5f`, `maxTickInterval = 2f`) — the first time `NpcAttendable` is constructed at runtime rather than hand-placed in the scene. Sets private `[SerializeField]` fields before the inactive-GO `Awake()` fires.
- **`ArrivalSpawner.FindSpawnPosition()` (Iteration 30)** picks a random side of `WorldLayoutGenerator.TownBounds` and places the arrival at a random distance (`minDistFromTown`–`maxDistFromTown`, defaults 2–6) outside that edge — arrivals spawn near the town edge, not the deep wilderness. Validates within `WildernessBounds` and not inside `TownBounds`; falls back to due-south of town center after 50 attempts.
- **Two `ArrivalTrigger` entries in `Greybox.unity` (Iteration 30)**: (1) "A Stranger" — flags `["bog_keeper_iron_sense"]`, `minimumDevelopedEntities: 3`; (2) "Another Traveler" — flags `["herald_trail_markers", "hedge_witch_wound_lore"]`, `minimumDevelopedEntities: 4`. Both `warnessThreshold: 3`, `npcProgressCost: 8`, tick intervals 1.5–2s.
- **`EntityFeedback` (Iteration 32)** (`Mossmark.Visuals`) is added to every entity that can produce signal. Three signals: (1) progress pulse — small scale-up-down animation (peak 1.3×, 0.18s) on `DevelopableEntity.OnProgressMade` / `WildernessYieldAttendable.OnProgressMade` / `TendedSpotAttendable.OnProgressMade`; (2) stage-cross shape swap — sprite transitions triangle→circle (one-way; stays circle for all further stage crosses) plus a larger pop (peak 1.65×, 0.35s) on `DevelopableEntity.OnDeveloped` — the circle is the persistent "this entity has developed" indicator, the pop is the per-cross moment signal; (3) passive drift halo — white circle child GO (scale 1.9×, alpha 0.35) shown on `NpcAttendable.OnPassiveDriftAccrued`, hidden on next productive progress tick. `suppressNextPulse` flag prevents the smaller pulse from firing alongside the pop on stage-cross ticks (since `OnDeveloped` fires before `OnProgressMade` in the same sync chain). `EntityFeedback` is added in all `WorldGenerator` spawn paths, `ArrivalSpawner.OnArrivalPromoted()`, and hand-placed to Old Cairn, Watcher's Post, Wanderer, Roughhand in Greybox.unity.
- **`CircleSpriteGenerator` (Iteration 32)** follows the same pattern as `TriangleSpriteGenerator`: `Initialize(color)` for spawn-time wiring, `static CreateSprite(color, textureSize)` for on-demand generation (used by `EntityFeedback` for shape swaps and the halo child). Both generators now expose their static factory so visuals can be re-created at runtime without requiring a full component add/remove cycle.
- **Player attention rock (Iteration 32)** (`Mossmark.Player`): `PlayerController.HandleAttentionRock()` polls `AttentionManager.State` each `Update()`. While `Attending`, rocks Z rotation ±`rockMaxDegrees` (5°) via sine wave at `rockFrequency` (1.5 Hz). Captures pre-attend rotation on the first attending frame; restores it via `Quaternion.RotateTowards` at `rockResetSpeed` (180°/s) when attention ends or is cancelled.
- **NPC post-spec stage costs (Iteration 33.1)**: stage 1 (common-item stages) `progressCost` is 6; stage 2 (rare-item stages) `progressCost` is 8. These are doubled from the Iteration 26 baseline (3 and 4 respectively) to give passive drift (Iteration 31) and cross-influence (Iteration 34) room to be felt before stages complete. Revisit after Iteration 34 play sessions. Touch only post-spec stages — building restoration costs (all 4) and specialization costs remain at their iteration-26 values.
- **`NpcStageDef.passiveDriftSourceSpotId` (Iteration 34 Seam 1, re-keyed in Iteration 38)**: replaces the Iteration 31 hardcode (and Iteration 34's `passiveDriftSourceArchetypeId`). Any post-spec stage can opt into rest-driven passive drift by setting this string to a `WildernessSpotDefinition.spotId`. `NpcAttendable.OnDayAdvanced()` looks up the spot via `WorldGenerator.GetSpot(spotId)`, reads its `Tendedness`, and applies 0/1/2 passive progress (0 if <0.3, 1 if ≥0.3, 2 if >0.7). Empty string = no passive drift (attention-only). Two live pairs: `bog_keeper_drainage ← bog`, `hedge_witch_wound_lore ← sacred_grove` (extracted archetype spots kept their former archetype id as `spotId`, so the data carried over unchanged).
- **`KnowledgeYieldEntry.requiredSpecializationId` (Iteration 34, Seam 2)**: parallel to the existing `requiredFlag`. `BuildKnowledgeInjectedYields()` checks either: if `requiredFlag` non-empty → check `WorldContext.GetFlag()`; else if `requiredSpecializationId` non-empty → check `WorldContext.IsSpecializationRealized()`. The two fields are mutually exclusive per entry — a single entry uses one or the other. Following the KnowledgeYieldModifier precedent (CLAUDE.md), the check stays in `WildernessYieldAttendable` (World namespace) to avoid cross-namespace deps. Live example: `hedge_witch` spec realized → Deep Wood Shrine injects Bark Strips (weight 0.13).
- **`BuildingStageDef.worldStateFlag` (Iteration 34, Seam 3)**: when non-empty, `BuildingAttendable.HandleDeveloped()` calls `WorldState.SetFlag(stages[CurrentStageIndex].worldStateFlag, true)` after each stage completes. This is the approved way for a building stage completion to affect wilderness yields (via `KnowledgeYieldEntry.requiredFlag` on the spot) or any other WorldState-gated system — no direct building→spot reference, the flag is the decoupling layer. Live example: Woodland Shrine (sacred_grove, stage 0) sets `shrine_tended`; Deep Wood Shrine's third `spotKnowledgeYield` entry reads that flag to inject Carved Sigil Stone (weight 0.08).
- **`PropertyRegistry` is a static class in `Mossmark.Inventory` (Iteration 35)**: 8 folk-phrase properties hard-coded as `PropertyDefinition[]` — not a ScriptableObject, since the vocabulary is small and authoring it in C# is simpler than asset-reference wiring. CSV pipeline can absorb ids/phrases as plain string columns in a future pass. `PropertyKnowledge` (static, `Mossmark.Development`) is the session-only discovery store — same flat dictionary shape as `WorldState`, accessible via `WorldContext.IsPropertyKnown(itemId, propertyId)`. Item properties are authored as `string[]` on `ItemDefinition.propertyIds` (YAML arrays in `.asset` files). `InventoryUI` shows known phrases in muted green below the item name; unknown properties render as the single fixed line "There's more to it." Debug: backtick (` `` `) calls `PropertyKnowledge.RevealAll()` and refreshes; F2 toggles `ShowDebugTags` (shows property ids instead of phrases). Property coverage: every property appears on ≥2 items — `heavy_true`: Bog Iron + Flat Stones; `holds_the_cold`: Bog Iron + Clay; `split_prone`: Reeds + Flint; `binds_fast`: Reeds + Bark Strips; `draws_the_eye`: Crow Feather + Raven's Eye; `keeps_well`: Mistletoe + Bark Strips; `turns_water`: Mistletoe + Clay; `burns_slow`: Flint + Sticks.
- **`ConversionDef` ScriptableObject (Iteration 36, `Mossmark.Inventory`)**: each `Input` entry has a `Kind` enum (`Item` or `Property`). Item-keyed inputs match a specific `ItemDefinition` + `quantity`; property-keyed inputs match any item carrying the named `propertyId`. `TryMatch(ItemDefinition[] placed, out int[] matchedSlots)` does greedy left-to-right matching — copies placed[] to a mutable remaining[], nulls matched entries to prevent double-use, returns false on first unsatisfied input. `matchedSlots[i]` is the last matched slot index for input `i`. Five live recipes: `recipe_plaited_cord` (3×Reeds → Plaited Cord), `recipe_tinder_bundle` (2×Bark Strips + 1×Sticks → Tinder Bundle), `recipe_weighted_snare` (1×binds_fast + 1×heavy_true → Weighted Snare), and from Iteration 39 `recipe_sealing_daub` (1×turns_water + 1×holds_the_cold → Sealing Daub) and `recipe_warding_charm` (1×draws_the_eye + 1×binds_fast → Warding Charm).
- **`WorkshopAttendable` (Iteration 36) was deleted in Iteration 39** — the Workshop is now a scene-placed `BuildingAttendable` with a single-stage `workshop_pool` (`workshop_restoration`, Flat Stones), which also means it participates in maintenance drift like every other building (the Iteration-36 "no `IMaintenanceConsumer`" carve-out no longer applies). The free/one-shot open semantics carried over: `lastAttentionWasOpen` guards `RequiresDaylight`/`ContinueAttending` in `BuildingAttendable`.
- **`WorkshopUI` slots are references, not removals (Iteration 36)**: placing an item into a WorkshopUI slot does NOT remove it from inventory. Items are consumed only at Work time on success (`HandleSuccess()`). This eliminates any need for a return-all-items path on Close(). `HandleSuccess()`: consumes all matched inputs via `InventoryManager.RemoveItem()`, marks consumed items' properties known *(Iteration 39: only those in the open station's bias)*, adds output item, posts flavor text, calls `EntityFeedback.TriggerPop()` on the station GO. `HandleFailure()`: finds all `(item, propId)` pairs across placed items where `!PropertyKnowledge.IsKnown()` *(Iteration 39: bias-filtered)*, picks one at random, marks it known, posts a notification — if no candidates remain, posts `"nothing comes of it."` Each Work attempt costs 1 daylight via `DayCycleManager.SpendDaylight()` regardless of outcome.
- **`WorkshopUI.IsOpen` guard (Iteration 36)**: `PlayerController.HandleInput()` and `AttentionManager.HandleHoldStarted()` both check `WorkshopUI.Instance != null && WorkshopUI.Instance.IsOpen` and bail early, following the same pattern as `ChestUI`. Add this guard to any future modal UI that captures input.
- **`PropertyAvailableCondition` (Iteration 37, `Mossmark.Development`)**: implements `IDependencyCondition` — checks `InventoryManager.Instance.Stacks` (carry-only, not chest) for any item whose `PropertyIds` contains the required `propertyId`. `GetNeedsDescription()` returns the authored `wantDescription` field (falls back to `"needs something that {phrase}"` via `PropertyRegistry.GetById()` if empty). *(Iteration 38 update: the `requiredPropertyId`/`wantDescription` stage-def fields this shipped with are gone — the condition itself is authored in the stage's generic condition list via `stage_conditions.csv`.)* `NpcAttendable` scans the applied stage's `Dependencies` for `PropertyAvailableCondition` and calls `ConsumePropertyMatchedItem(want.PropertyId)` — removes one item from carry, picking the stack with the highest quantity (most common); the overlay's always-visible want line comes from `FindPropertyWant(nextStage)`. Live instance: `bog_keeper_drainage` (Bog Keeper) gates on `turns_water`; overlay reads "needs something that turns water for the drainage channels." `bog_keeper_iron_sense` remains exact-item for contrast. Content path verified: both Mistletoe and Clay carry `turns_water` and are discoverable at the Workshop before Bog Keeper specializes.
- **Relational data architecture (Iteration 38)** — the authoring layer mirrors P1's relational pipeline; [DATA_SCHEMA.md](DATA_SCHEMA.md) is the file-by-file reference. Standing patterns:
  - **`PlaceArchetype` is a thin composition root**: it references `WildernessSpotDefinition[] spots`, an `NpcStagePool`, and a `BuildingStagePool` rather than embedding their data. Only genuinely archetype-level data (identity, specialization, POI block, exchange flavor, maintenance) lives inline. `npcMaintenanceMaterial` is an explicit `ItemDefinition` reference (was `CommonYields[0]` positionally).
  - **Stage gates are `[SerializeReference] IDependencyCondition[]` on the stage SOs** (`NpcStageDef.conditions`, `BuildingStageDef.conditions`), authored via `stage_conditions.csv` → `ConditionCsvImporter`. The old ad hoc fields (`useRareItem`/`itemCount`/`requiredPropertyId` on NPC stages, `requiredSpecialization` on building stages) are gone. Structural gates stay derived in code and are NOT authored: the archetype's spec-realized gate on NPC post-spec stages (prepended by `BuildPostSpecStages`) and the material-availability gate on building stages (derived from `material`/`costPerTick`).
  - **`ConditionCsvImporter` is the standing pattern for condition-bearing data**: a `conditionType` discriminator column drives construction of the concrete condition object, assigned via `SerializedProperty.managedReferenceValue` (idempotent — equivalent arrays are left untouched to avoid managed-reference-id churn). New condition types need a case in `BuildCondition`, a case in `ExportGameData.AppendConditionRows`, `[Serializable]` + `[SerializeField]` (non-readonly) fields on the class, and a `_sample_conditions.csv` row (validated via `Mossmark/Data/Validate Condition Import`).
  - **Pool order is semantic**: import rebuilds each pool's `stages` array from CSV row order; a building pool's first row is its revival stage. `ExportGameData` therefore emits stage rows in pool order (`OrderByPools`), never alphabetically.
  - **Import creates assets on demand** (`LoadOrCreate`, P1's "one SO per row" behavior) and is a no-op on unchanged CSVs — re-running `Import All` twice must report 0 assets updated. Export (`Export All`) drops `#` comment lines from CSVs; durable authoring notes belong in DATA_SCHEMA.md.
  - **Rare yields are weighted pools** (`ItemYield[] rareYields` through `ItemYieldRoller.Roll`): the chance gates *whether* a rare drops, the pool decides *which*. `YieldTable` (ID-addressable, P1's LootTable analogue) can replace a spot's inline common/rare list when a pool is genuinely shared (`old_coin_finds` for Clay Pit + Field) — don't force tables where content isn't shared.
  - **`WorldGenerator.GetSpot(spotId)`** replaces `GetArchetypeSpot(archetypeId)`: any spawned Generic spot with a non-empty `WildernessSpotDefinition.spotId` registers; last spawned instance wins a contested id.
- **Station-ness is stage data (Iteration 39)**: `BuildingStageDef.biasPropertyIds` (plus optional `stationName`) on a pool's final stage makes the fully-developed `BuildingAttendable` a conversion station — no station component, no new gate type. When fully developed and station-capable, attending opens `WorkshopUI` (free, one-shot via `lastOutcomeKind == AttentionOutcomeKind.Open`, Iteration 41's successor to the old `lastAttentionWasOpen` bool) instead of the linger flavor; maintenance keeps priority 1. If `stationName` is set, the building's `DisplayName` becomes it once the station opens (Woodland Shrine → Consecrated Hearth). Three live stations: Workshop (scene-placed, `workshop_pool`), Fen Shrine (bog building, new `bog_fen_shrine` stage, Clay), Consecrated Hearth (sacred_grove building, existing `sacred_grove_hearth` stage). Bog/grove stations exist only in sessions where their archetype is selected — accepted as differentiation, the Workshop is always present. *(Iteration 40 update: "final stage" below is now "most recently applied stage" — see next bullet.)*
- **`IWorkStation` lives in `Mossmark.Inventory` (Iteration 39)**: the station contract (`StationDisplayName`, `BiasPropertyIds`, `StationObject`) is defined UI-side so `WorkshopUI` never references `Mossmark.Development` — same directionality rationale as the `KnowledgeYieldModifier` precedent. `BuildingAttendable` implements it.
- **Bias filters both recipe resolution and discovery (Iteration 39)**: `WorkshopUI.IsRecipeInStationBias()` — a property-keyed recipe needs **all** its property inputs in the station's bias; an item-keyed recipe needs any of its input items' latent properties in the bias (items with no properties = available everywhere). Success and failure discovery only ever reveal bias properties — an off-bias property can't be learned at the wrong station, which is what makes station choice a decision. Coverage audit (documented in `building_stages.csv` comments): every one of the 8 properties is in exactly 2 stations' bias; recipe partition — Plaited Cord/Tinder Bundle everywhere, Weighted Snare Workshop-only, Warding Charm Workshop+Fen Shrine, Sealing Daub Fen Shrine+Hearth.
- **Station capability is keyed off the most recently applied stage, not the literal last array entry (Iteration 40)**: `BuildingAttendable.AppliedStationStage` searches `stages[0..CurrentStageIndex]` from the end for the first stage with non-empty `biasPropertyIds`, replacing Iteration 39's `FinalStage`/`IsStationOpen` (which required `GetNextStage() == null`, making "station-capable" and "has more stages to develop" mutually exclusive by construction). `DisplayName`'s station-name substitution and `StationDisplayName`/`BiasPropertyIds` all read `AppliedStationStage` now. `CanAttend()` gate: `GetNextStage() == null || CanMakeProgress() || IsStationCapable` — a station-capable building with pending stages the player can't currently progress stays attendable for the station instead of locking out. `OnAttentionComplete()` priority: (1) maintenance [unchanged] → (2) develop, if `CanMakeProgress()` [forward progress wins when circumstances allow it] → (3) else if `IsStationCapable`, open the station → (4) else if `GetNextStage() == null`, flavor linger. `GetOverlayInteractionLine()` mirrors the same priority so the hover text never promises an outcome attending won't actually produce. No authored content changed — every station stage today still happens to be the final row in its pool, so live behavior is unchanged; the mechanism was verified by code-path trace rather than new authored content (see FEATURES.md Iteration 40 for the case-by-case trace).
- **`AttentionOutcomeKind` + `BuildingAttendable.PredictOutcomeKind()` formalize Iteration 40's priority chain into one method (Iteration 41)**: `Mossmark.Development.AttentionOutcomeKind` (`Develop`, `Open`, `Maintain`, `Visit`) is its own file — shared as a type even though only `BuildingAttendable` uses the resolution logic this iteration, since enums are free to share. `PredictOutcomeKind()` runs the exact same maintenance → develop-if-`CanMakeProgress()` → open-if-`IsStationCapable` → else-visit chain, and is now the single call site both `OnAttentionComplete()`'s dispatch (a `switch`, replacing the old if/else-if bodies) and `RollTickInterval()` use — `CanAttend()` was also rewritten against it (`kind != Visit || GetNextStage() == null`, verified equivalent to the Iteration 40 chain for all four kinds). The three `lastAttentionWasVisit`/`lastAttentionWasMaintenance`/`lastAttentionWasOpen` bools are gone, replaced by one `lastOutcomeKind` field read after `OnAttentionComplete()` (same "read after complete" timing rule as ever) — `RequiresDaylight`/`ContinueAttending` now just check `lastOutcomeKind == AttentionOutcomeKind.Develop`. `GetOverlayInteractionLine()` keeps its own independent (already-correct) chain rather than switching to `PredictOutcomeKind()` — out of scope for this pass.
- **Duration and daylight cost run through `OutcomeRequest`/`IOutcomeModifier` (Iteration 41)**: `OutcomeRequest` gains `DurationMultiplier = 1f`, parallel to `ChanceMultiplier`/`DaylightCostMultiplier` — a separate dimension because duration is a continuous organic min/max roll and daylight is a discrete integer currency; one is never derived from the other. `BuildingAttendable.BuildOutcomeRequest()` constructs one `OutcomeRequest` and runs `DriftColdDaylightModifier(DriftProgress, DriftThreshold)` (new `IOutcomeModifier`, takes raw ints rather than an entity reference — same decoupled-constructor precedent as `WorldStateChanceModifier`/`WoundLoreModifier`) through it; this doubles `DaylightCostMultiplier` once cold, replacing the old inline `if (DriftProgress >= DriftThreshold) SpendDaylight(1)` — since the Develop base cost is always 1, ×2 and +1 land on the same number, so this is a pure refactor, not a balance change. `RollTickInterval()` picks `minTickInterval`/`maxTickInterval` for `Develop`/`Visit` or the new `minInteractInterval`/`maxInteractInterval` (default 0.5–1s; explicit on the scene-placed Workshop, script default for procedural stations) for `Open`/`Maintain`, then scales the roll by `DurationMultiplier`. No `IAttendable` interface change — `RequiresDaylight` stays a plain bool; `BuildingAttendable` self-spends only the amount beyond the base 1 that `AttentionManager` already spends via `RequiresDaylight`. No rollout beyond `BuildingAttendable` this iteration.
- **Site Clustering pilot (Iteration 42, `Mossmark.World`)**: `WorldGenerator.SpawnArchetypeSites()` replaces the old separate `SpawnWildernessSpots()`/`SpawnPois()` loops — one anchor point (`FindValidPosition()`) is drawn per selected archetype, then every one of that archetype's `Spots` plus its POI place via the new `FindValidPositionNear(anchor)` (jitters within `siteJitterRadius`, default 3, while still honoring `minSeparation`/wilderness-vs-town bounds). Both position methods now share one rejection-sampling core, `FindValidPositionInternal(Func<Vector2> candidateGenerator, int maxAttempts)`. This clustering applies to every archetype generically, not just Bog.
- **`PlaceArchetype.PoiDormantByDefault` / `.PoiRevealWorldStateFlag` (Iteration 42)**: a dormant-by-default POI is skipped at world-gen spawn; `WorldGenerator` remembers its archetype + anchor in `dormantPois` and checks `PoiRevealWorldStateFlag` against `WorldState` on every `DayCycleManager.DayAdvanced` tick (`CheckDormantSiteReveals()`), spawning the POI clustered near its anchor (via `FindValidPositionNear`) the first time the flag reads true. Both fields default off/empty and are intentionally **not** in the CSV pipeline (`ExportGameData`'s field list) — authored by hand only on `archetype_bog.asset`, which reuses the *existing* `bog_keeper_drainage` stage-completion flag (`NpcStageDef.worldStateFlag`, Iteration 34 Seam 3) as its reveal trigger rather than inventing a new one. No new `Site` schema — this is two plain fields on the existing composition root, per the iteration's explicit scope guard.
- **`HintFlavorEntry` (Iteration 42, `Mossmark.World`, now defined alongside `KnowledgeYieldEntry`/`ItemYield` in `WildernessSpotDefinition.cs`)**: the same shape as `KnowledgeYieldEntry` (gate on a `WorldState` flag via `WorldContext.GetFlag`) but delivers a flavor line through `NotificationManager.Post` instead of injecting an item into the yield pool. Authored per-spot on `WildernessSpotDefinition.hintFlavors[]`; rolled via the shared `ItemYieldRoller.TryFireHintFlavor()` static (at most one per tick) from both `WildernessYieldAttendable.OnAttentionComplete()` (POIs) and `DevelopingWildernessSpotAttendable.OnAttentionComplete()` (Generic spots). `NpcAttendable.OnDayAdvanced()`'s passive-drift branch sets a `$"{nextStage.Id}_started"` `WorldState` flag whenever it adds passive progress — a generic, reusable "this stage has some progress on it" signal (not bog-specific in code) that the one authored hint (`spot_fen_bog.hintFlavors[0]`, gated on `bog_keeper_drainage_started`) reads.
- **`DevelopingWildernessSpotAttendable` extends `DevelopableEntity` directly, not `WildernessYieldAttendable` (Iteration 43 pilot, `Mossmark.World`)**: the two base classes have incompatible progress models (continuous `tendedness` drift vs. threshold/stage bookkeeping) and C# has no multiple inheritance, so Generic wilderness spots needed a genuine fork rather than a flag on the existing class. Iteration 43 piloted this on Fen Bog alone (opt-in via `WildernessSpotDefinition.spotStagePool`, null everywhere else); Iteration 44 generalized it to every Generic spot and **retired `GenericWildernessSpotAttendable`** entirely — `WorldGenerator.SpawnSpotFromDefinition()` now spawns `DevelopingWildernessSpotAttendable` unconditionally for Generic kind, no null-check branch. `PoiAttendable` is the only class still extending `WildernessYieldAttendable` (continuous tendedness), since POIs were explicitly out of scope for the rollout. Knowledge-yield injection and hint-flavor delivery (Iterations 28/42) were extracted from `WildernessYieldAttendable` into public statics on `ItemYieldRoller` (`BuildKnowledgeInjectedYields()`, `TryFireHintFlavor()`) specifically so both classes could keep those existing features without duplicating the logic — Fen Bog's `knowledgeYields`/`hintFlavors` content (the `bog_keeper_iron_sense` injection, the `bog_keeper_drainage_started` hint) still fires unchanged under the new class.
- **Exhaustion (session-scoped) vs. Standing (latched) are deliberately two different mechanisms, not two bands of one float (Iteration 43, generalized to all Generic spots in Iteration 44)**: `exhaustion` is a plain float incremented per attended tick and zeroed every `OnDayAdvanced()`. Its penalty is two stacking tiers (re-tuned in Iteration 43.1 — see below): past `exhaustionPenaltyThreshold` (0.6) attending costs double daylight/duration; only once fully overworked (exhaustion ≥ 1.0) does it also feed `ItemYieldRoller.Roll()` the same synthetic `<0.3` proxy value the old depleted `tendedness` band used, reusing the existing qty/chance penalty shape rather than inventing a new one. Standing is a `SpotStageDef`/`SpotStagePool` track (mirrors `NpcStageDef`/`NpcStagePool`) gated by new `SustainedGoodAttentionCondition(minDays)`. That condition reads a new `IGoodAttentionTracker.GoodAttentionDays` interface (`Mossmark.World`) rather than a `DevelopableEntity` field, so the World-scoped counter concept doesn't leak into `Mossmark.Development` — same cross-namespace discipline as `ArrivalCondition`. `goodAttentionDays` increments in `OnDayAdvanced()` only when that day was attended and not overworked; an overworked day fails to advance it but does not reset it, so exhaustion costs a day's yield without ever undoing progress toward Familiar. Attends call `AddProgress()`/`TryApplyStage()` directly instead of `ResolveAttention()` — wilderness yields must happen every tick regardless of whether Standing's gate is currently satisfied (unlike Buildings/NPCs, where "dependencies unsatisfied" blocks the productive-tick signal entirely) — so a new `DevelopableEntity.RaiseProgressMade()` protected helper lets the subclass raise the progress-pulse feedback event directly, since C# events can only be invoked from their declaring class.
- **`ITendednessSource` (Iteration 43, `Mossmark.World`, on `WildernessYieldAttendable`)** keeps `WorldGenerator.GetSpot(spotId)`'s registry — and by extension the Bog Keeper's Iteration 34 passive-drift seam, which reads `bog`'s tendedness — working for spots that aren't `WildernessYieldAttendable` at all. `DevelopingWildernessSpotAttendable` implements the same interface with a synthetic reading (0.2 while overworked, 0.85 once Familiar, 0.5 baseline — re-keyed from "past exhaustion threshold" to "overworked" in Iteration 43.1, see below) so that seam's behavior is preserved rather than silently going dark; `spotsById`'s dictionary value type is the interface, not a concrete class, precisely so this kept working once every Generic spot's concrete type changed in Iteration 44.
- **`ExhaustionCostModifier` (Iteration 43.1, `Mossmark.Development`)**: doubles both `DaylightCostMultiplier` and `DurationMultiplier` once a spot's `exhaustion` crosses `exhaustionPenaltyThreshold` — the "felt, not read" fix for a problem found playtesting Iteration 43: a quiet yield dip at the threshold didn't read as a real warning, and if anything read as tolerable enough to keep pushing. Cost/duration are things the *verb itself* makes unmissable, so they became the threshold-crossing signal instead of text; the existing yield penalty (qty -1, rare chance ×0.7) was demoted to only apply once exhaustion reaches full overwork (1.0), stacking on top of the still-doubled cost/duration rather than replacing it. Same decoupled-constructor shape as `DriftColdDaylightModifier` (raw `exhaustion`/`threshold` floats, not an entity reference). `DevelopingWildernessSpotAttendable.BuildOutcomeRequest()` mirrors `BuildingAttendable.BuildOutcomeRequest()` exactly: one `OutcomeRequest`, self-spend the daylight beyond the base 1 `AttentionManager` already spent, and multiply `RollTickInterval()`'s roll by `DurationMultiplier`. Piloted on Fen Bog only; generalized to every Generic spot in Iteration 44 along with everything else `DevelopingWildernessSpotAttendable` does.
- **`RollTickInterval()` must be re-rolled on `OnDayAdvanced()`, not just at the end of each attend (Iteration 43.1 post-ship fix)**: `currentTickInterval` is a cached field, and the only two call sites that refreshed it were `Awake()` and the end of `OnAttentionComplete()` — so a doubled duration baked in during the last exhausted attend of a day sat there, stale, until the *next* attend completed, meaning the first attend of the following day still took twice as long even though `exhaustion` had already reset to 0. `OnDayAdvanced()` now calls `RollTickInterval()` itself after the reset. Daylight cost has no equivalent bug — `BuildOutcomeRequest()` reads `exhaustion` fresh every time `OnAttentionComplete()` runs, nothing caches it. General lesson: any cached duration/cost value derived from session-scoped state must be re-derived at every point that state resets, not just at the state's own natural update points.
- **`SpotStageDef.tint` (Iteration 43 post-ship fix)**: `TriangleSpriteGenerator`/`CircleSpriteGenerator` bake color into the sprite texture itself rather than reading `SpriteRenderer.color`, which Unity otherwise leaves at its default white. `BuildingAttendable.UpdateVisual()` already accounts for this by setting `spriteRenderer.color = stages[idx].tint` per stage; `DevelopingWildernessSpotAttendable` didn't, so `EntityFeedback`'s stage-cross shape swap (which bakes its new circle sprite *from* `SpriteRenderer.color`) baked a white circle on crossing Familiar. Fixed the same way: `SpotStageDef` gained a `tint` field, and `DevelopingWildernessSpotAttendable.HandleDeveloped()` sets `spriteRenderer.color = def.Tint` before returning — it fires first because it subscribes to `OnDeveloped` in `Awake()`, and `EntityFeedback` subscribes in `Start()`, the same ordering guarantee the Buildings comment above already documents. Any future `DevelopableEntity` subclass whose sprite is generated by `TriangleSpriteGenerator`/`CircleSpriteGenerator` needs the same per-stage `SpriteRenderer.color` write, or its stage-cross shape swap will silently lose tint the same way.
- **Iteration 44 generalized Iteration 43's Fen Bog pilot to all 10 Generic wilderness spots** — same single-pilot-then-generalize shape as 31→34, 36→39, 39→40. Two scope decisions settled before implementation (asked of, and answered by, the user rather than assumed): (1) **Generic spots only** — Tended spots (Bee Skep, Bramble Patch) and POIs stay on their existing models entirely; (2) **Standing is a generic template, not bespoke per spot** — every spot's Familiar stage uses identical `progressCost`/condition/`rareChanceMultiplier`/flavor text, with only `tint` varying, and even that's derived by one formula (lerp the spot's base `color` 30% toward white) rather than hand-picked per spot. This is a deliberate content-quality tradeoff: no per-spot creative writing now, in exchange for full coverage; a future pass can hand-tune specific spots' Standing once this baseline is played, since overriding one CSV row doesn't touch the other nine. `SpotStageDef`/`SpotStagePool` also earned CSV pipeline support this iteration (`spot_stages.csv`, mirroring `npc_stages.csv`/`building_stages.csv` exactly, importing before `wilderness_spots.csv` since spots now reference stage pools by name) — the single-pilot "doesn't earn CSV support yet" reasoning from Iteration 43 no longer applies once the schema covers 10 real rows.
- **`PoiTier` (Iteration 45, `Mossmark.World`)** generalizes Iteration 42's plain dormant/spawned bool into `Hidden`/`VisibleInert`/`Interactable`. `Hidden` is a pre-spawn-only state — `WorldGenerator`'s `dormantPois` list is the sole place it's tracked; once a POI's `GameObject` exists, `PoiAttendable`'s own runtime state only ever spans `VisibleInert` → `Interactable` and never reverts. `PlaceArchetype.PoiStartingTier` (default `VisibleInert`) replaces `PoiDormantByDefault`; `PoiRevealCondition`/`PoiUnlockCondition` ([SerializeReference] `IDependencyCondition`, hand-authored, not CSV-wired — same discipline as Iteration 42/43's single-pilot fields) replace the old bare `PoiRevealWorldStateFlag` string and the previously-hardcoded `SpecializationRealizedCondition` unlock gate. A null `PoiUnlockCondition` falls back to that original gate in `WorldGenerator.SpawnPoi()`, so every archetype except this iteration's two pilots keeps identical unlock behavior. The one deliberate behavior change applies to every POI, not just the pilots: per the tier's own definition ("present, not interactable"), `PoiAttendable` now disables its sibling `Collider2D` while `VisibleInert` (polled and permanently re-enabled in `Update()` once `unlockCondition` is satisfied) — an inert POI never enters `AttendableDetector` range and shows no hover tooltip at all, whereas before this iteration a locked POI still displayed its `lockedDescription`/needs text on hover. `SpotStageDef` gained a `WorldStateFlag` field mirroring `NpcStageDef`/`BuildingStageDef`'s (set by `DevelopingWildernessSpotAttendable.HandleDeveloped()` on stage cross) specifically so a spot's Standing crossing could bridge into a POI's `unlockCondition` via the existing `WorldStateCondition` type — the Reed Marsh Blind pilot's "nearby spot reaching Standing" wilderness-tied unlock needed no new condition type, just this one flag-bridge field extended from Buildings/NPCs to Spots. The Fen Bog Hollow pilot's harder unlock similarly reuses an existing flag rather than inventing anything: `bog_keeper_iron_sense` (the Bog Keeper's second, deeper post-spec stage) already sets its own `WorldStateFlag` on completion.
- **Iteration 46 rolled Iteration 45's POI tiering out to the remaining 3 archetypes** (Old Road, Sacred Grove, Old Quarry) — pure data authoring against the existing mechanism, no new condition type, nothing added to the CSV pipeline. Old Road (`archetype_old_road.asset`): `poiStartingTier: Hidden`, `poiRevealCondition = WorldStateCondition(old_road_familiar)` (new flag added to `old_road_familiar.asset`, the archetype's own spot's Standing stage), `poiUnlockCondition = WorldStateCondition(herald_toll_records)` (Herald's existing deeper post-spec stage flag — already set, no NPC-side authoring needed). Sacred Grove (`archetype_sacred_grove.asset`): `poiStartingTier: VisibleInert` (the field default, set explicitly for parity with Reed Marsh's asset), no reveal condition, `poiUnlockCondition = WorldStateCondition(hedge_witch_ravens_eye)` (Hedge Witch's existing deeper post-spec stage flag). Old Quarry (`archetype_quarry.asset`) is the one fully-wilderness-tied pair, since Stonemason has no post-spec stages (Iteration 33.1): `poiStartingTier: Hidden`, `poiRevealCondition = WorldStateCondition(old_quarry_familiar)` (the archetype's own spot) and `poiUnlockCondition = WorldStateCondition(stone_outcrop_familiar)` (a second, *different* Generic spot — Stone Outcrop is not archetype-owned, it spawns from `WorldGenerator`'s random `spotPool`, chosen for thematic fit over the other pool spots) — two independent 3-day Standing investments back to back, clearing "meaningfully harder than Familiar" by construction alone. All three `SpotStageDef.worldStateFlag` fields were empty before this iteration (the field itself shipped in Iteration 45); this iteration only sets values on it. Because Stone Outcrop is drawn randomly rather than guaranteed, Old Quarry's POI is subject to the same reachability caveat as the existing Open Thread on single-sourced content (FEATURES.md) — accepted here since the design brief explicitly calls Old Quarry "the only fully-wilderness pair" and doesn't ask for a reachability guarantee.
