# Mossmark — Project Guide

## Project Goal

A top-down 2D RPG focused on **world and town development** rather than player character progression. The player moves through the world and holds a single **Attention** action on whatever is nearby — what happens is a property of the thing being attended to, not a menu choice. Sustained attention develops NPCs, buildings, and the town over time, revealing new specializations and access rather than presenting an upgrade menu. Tech is per-town and must be manually transferred between towns in later phases.

See [IDEAS.md](IDEAS.md) for the high-level vision and [PROTOTYPE2.md](PROTOTYPE2.md) for the current prototype design and iteration roadmap.

> **Note:** This is "Prototype 2" — a fresh Unity project started from scratch (see PROTOTYPE2.md's "Why a second prototype"). It is informed by Prototype 1 but does not carry over P1's architecture; P1 systems referenced in PROTOTYPE2.md (e.g. `PlayerMovement`, `DayCycleManager`, `Entity`/`UpgradePool`) are reference material from the old project, not code that exists here yet.

---

## Environment

- **Unity version**: 6000.4.11f1 (Unity 6)
- **Render pipeline**: URP, 2D Renderer (`Assets/Settings/Renderer2D.asset`). The active scene has a Global Light 2D — 2D lights are available and may be used later for day/night ambience (the daylight/day cycle in PROTOTYPE2.md).
- **Physics**: 2D (`Rigidbody2D`, gravity = 0 for top-down movement)
- **Input**: new Input System (`com.unity.inputsystem`), via the project-wide actions asset `Assets/InputSystem_Actions.inputactions`. See "Input Actions" below.
- **UI system**: Unity UI Toolkit (`UIDocument`, `VisualTreeAsset`, `VisualElement`) — **not** uGUI/Canvas
- **Editor tooling**: `com.gamelovers.mcp-unity` is installed for MCP-based editor automation (not yet wired into this session's tools).

---

## Input Actions

`Assets/InputSystem_Actions.inputactions` is configured as the project-wide actions asset (Project Settings → Input System Package). Access it via `InputSystem.actions`, enabling the relevant map in `OnEnable`:

```csharp
var gameplay = InputSystem.actions.FindActionMap("Gameplay");
gameplay.Enable();
var move = gameplay.FindAction("Move");
```

- **`Gameplay` map**
  - `Move` — `Vector2`, WASD / arrow keys / gamepad left stick. Drives `PlayerController`.
  - `Attend` — `Button` with a `Hold` interaction (keyboard `E`, gamepad North button). Drives `AttentionInput` (see Key Design Decisions for how its `started`/`canceled` callbacks are used).
  - `Horizon` — plain `Button` (keyboard `Tab`). Toggles the Settlement Horizon panel (`HorizonUI`).
- **`UI` map** — retained from the default template for future menu surfaces (e.g. the settlement chest, the one menu-based interaction PROTOTYPE2.md keeps).

---

## Code Conventions

### Language and approach
- **C# only** — no visual scripting (no Bolt, no Unity Visual Scripting graphs)
- **Code-first**: all game logic lives in MonoBehaviours, plain C# classes, and ScriptableObjects
- **1st-party APIs first**: prefer Unity's built-in systems over 3rd-party plugins for gameplay code
- **No 3rd-party gameplay frameworks**

### Namespaces
All scripts live under the `Mossmark` root namespace, organized by system. This table reflects PROTOTYPE2.md's "Core Systems" section, generalized from Prototype 1:

| Namespace | Purpose |
|---|---|
| `Mossmark.Player` | Player controller, camera follow, input relay |
| `Mossmark.Visuals` | Shared grey-box visual primitives (e.g. `TriangleSpriteGenerator`) used across systems |
| `Mossmark.Attention` | Attention framework (zones, detector, manager, overlay UI) — generalizes P1's Interaction framework (`IInteractable` → `IAttendable`) |
| `Mossmark.Day` | Daylight/day-clock — phase tracking, ambient text, and the daylight HUD. Direct successor to P1's `DayCycleManager` |
| `Mossmark.Inventory` | Inventory manager, item database, item pickups, settlement chest |
| `Mossmark.Development` | Generic dependency/response resolver and developable entities (buildings, NPCs, POIs, town) — generalizes P1's `Entity`/`UpgradePool`/`TownEntity` |
| `Mossmark.World` | Region and town generation, place archetypes, and generic wilderness-spot attendables (e.g. `GenericWildernessSpotAttendable`) |

P1's `Mossmark.Quests` and `Mossmark.Combat` are **not** part of P2 — collection quests and discrete combat encounters were dropped per IDEAS.md's "Prototype 2" section. Add namespaces here as new systems land; keep this table in sync with what's actually implemented.

### Tooling language default
- **Default to C# for all project tooling** (Editor scripts, build helpers, data importers). Unity's `AssetDatabase`, `SerializedObject`, and `EditorUtility` APIs give direct, type-safe access to all game data without external dependencies. Only reach for another language when there is a concrete reason C# cannot do the job (e.g., a CI script that must run outside the Unity process and has no natural C# equivalent).
- Editor scripts live in `Assets/Editor/` and use `[MenuItem]` for discoverability inside the Editor. They are excluded from player builds automatically.

### Architecture rules
- **Event-driven**: systems communicate via `UnityEvent<T>` or C# `event Action<T>`, not direct method calls across system boundaries
- **Singleton managers**: one per system (e.g., `AttentionManager.Instance`), implemented as `MonoBehaviour` singletons with `Awake` guard
- **UI holds no game logic**: UI components only display state pushed from managers; they never write to game state directly
- **Interfaces for contracts**: use interfaces (e.g. `IAttendable`) for cross-system contracts so systems stay decoupled
- **ScriptableObjects for data**: item definitions, development stages, region/place-archetype data, loot tables — all as `ScriptableObject` assets stored under `Assets/Game/Data/`. Do **not** use `Assets/Game/Resources/` for new assets.
- **Procedural spawning — inactive-GO pattern**: when creating GameObjects at runtime, call `SetActive(false)` immediately after `new GameObject()`, add all components and call their `Initialize()` methods, then `SetActive(true)`. This ensures `Awake` fires with correct data already set on serialized fields. Components that participate in procedural spawning must expose a public `Initialize()` method.
- **Each iteration leaves prior systems functional**: never break existing systems when adding new ones

### Comments
Default to no comments. Only add a comment when the *why* is non-obvious (a hidden constraint, a Unity quirk, a workaround). Never describe what the code does — good naming handles that.

---

## Active Scenes

- **`Assets/Game/Scenes/Overworld.unity`** — the active prototype scene; all new work targets this scene
- **`Assets/Settings/Scenes/URP2DSceneTemplate.unity`** — Unity's URP 2D scene template (used when creating new scenes via the Editor); not part of gameplay

---

## System Overview (Prototype 2)

| System | Status | Key Types |
|---|---|---|
| Input + Movement | Complete | `PlayerController`, `CameraFollow` |
| Grey-box Visuals | Complete | `TriangleSpriteGenerator` |
| Attention Framework Core | Complete | `IAttendable`, `AttendableZone`, `AttendableDetector`, `AttentionInput`, `AttentionManager` |
| Attendable Overlay UI | Complete | `AttendableOverlayUI` |
| Generic Wilderness Spots | Complete | `GenericWildernessSpotAttendable`, `ItemYield` |
| Inventory + HUD | Complete | `ItemDefinition`, `InventoryManager`, `InventoryUI` |
| Daylight / Day Clock | Complete | `DayCycleManager`, `DayCycleAmbientTextData`, `DayCycleUI` |
| Bedroll + Day Transition | Complete | `BedrollAttendable`, `DayTransitionFadeUI`, `DayCycleManager.Rest()` |
| Tended-Style Spots | Complete | `TendedSpotAttendable` |
| Generic Dependency / Response Resolver | Complete | `IDependencyCondition`, `DevelopmentStage`, `DevelopmentTrack`, `DevelopableEntity`, `WorldState` |
| Continuous Attention Framework | Complete | `IAttendable.ContinueAttending`, `AttentionManager` tick loop |
| Buildings: Revival via Attention | Complete | `BuildingAttendable` |
| NPCs: Development + Specialization Draw | Complete | `NpcAttendable` |
| Building → NPC Demand Loop | Complete | `DeclaredSpecializationNeeds`, `SpecializationNeededCondition` |
| World Gen: Place Archetypes | Complete | `PlaceArchetype`, `RegionData`, `WorldGenerator` |
| POIs | Complete | `PoiAttendable`, `RealizedSpecializations`, `SpecializationRealizedCondition`, `ItemYieldRoller` |
| Wandering Things | Complete | `WanderingThingAttendable`, `WanderingThingSpawner` |
| Settlement Chest (Rebuilt) | Complete | `ChestAttendable`, `ChestUI` |
| Settlement Horizon UI | Complete | `HorizonUI` |
| World/Town Layout (Visual Polish) | Complete | `WorldLayoutGenerator`, `SquareSpriteGenerator` |
| NPC Post-Specialization Tracks | Complete | `WoundLoreModifier`, `WorldStateChanceModifier` (+ `DevelopableEntity.MarkStageAsApplied`, `OutcomeRequest.DaylightCostMultiplier`) |
| Wilderness Depth + Random Placement | Complete | `WildernessSpotDefinition`, `TendedSpotAttendable.Initialize()`, `WorldGenerator.FindValidPosition()` |
| Building Data Generalization (G1+G2) | Complete | `BuildingStageDef`, `BuildingAttendable` stages array, `PlaceArchetype` building block |
| NPC Post-Spec + Spot Tick Tuning (G3+G6) | Complete | `NpcPostSpecStageDef`, `PlaceArchetype.NpcPostSpecStages`, `WildernessSpotDefinition` tick intervals |
| Wandering Thing Definition (G4+G5) | Complete | `WanderingThingDefinition`, `WorldStateOddsModifier`, `WanderingThingSpawner` pool, `traveler.asset` |
| Code Quality Pass (G7+G8+G9) | Complete | `WildernessYieldAttendable` base class, `LandmarkAttendable`, `TendedSpotAttendable.harvestYields[]` |
| CSV / Data Pipeline | Complete | `Assets/Editor/ExportGameData.cs`, `Assets/Editor/ImportGameData.cs` |

Full iteration plan is in [PROTOTYPE2.md](PROTOTYPE2.md). Update this table as each iteration lands.

---

## Key Design Decisions

This section records decisions made during implementation that aren't already captured in PROTOTYPE2.md. Populate it iteration-by-iteration, the way the old CLAUDE.md did for Prototype 1.

- **Render pipeline & input, locked in at bootstrap**: kept the Unity 6 2D template's URP 2D Renderer (2D lights available for the later day/night cycle) and the new Input System with project-wide actions (see "Input Actions" above), rather than reverting to built-in 2D / legacy `Input.GetAxis` as the old CLAUDE.md (written for P1) specified.
- **`PlayerController`/`CameraFollow`/`TriangleSpriteGenerator`** ported from P1's `PlayerMovement.cs`, `CameraFollow.cs`, `TriangleSpriteGenerator.cs`, dropping the `CombatManager`/`DayCycleManager`/`RestInteractable` hooks that don't exist in P2 yet (per PROTOTYPE2.md, Section 1: "Drop the `CombatManager`/exhaustion-collapse hooks").
- **`AttentionInput` uses the `Attend` action's `started`/`canceled` callbacks, not `performed`.** The Hold interaction's `started` fires on press and `canceled` fires on release regardless of the interaction's own (default 0.4s) duration, so these give clean raw press/release signals. `AttentionManager` owns the actual hold-duration timing per-target via `IAttendable.AttentionDuration`, so the Hold interaction's configured duration is present but unused — left as-is rather than switching to a plain Button interaction, since `started`/`canceled` already give the needed signals.
- **`AttendableZone` resolves `IAttendable` via `GetComponent<IAttendable>()`** on its own GameObject rather than a serialized interface reference — every attendable type is expected to carry its `IAttendable` component and an `AttendableZone` on the same GameObject, which fits the inactive-GO procedural spawn pattern for later iterations.
- **Player's attention-range trigger doubles as its required `Collider2D`**: a `CircleCollider2D` (trigger, radius 1.5) was added directly to the Player in the editor for `AttendableDetector`'s range queries; `PlayerController.Awake()`'s fallback `AddComponent<CircleCollider2D>()` now finds this one and skips adding a second.
- **`AttentionManager` finds `AttendableDetector`/`AttentionInput` via `FindAnyObjectByType` in `Start()`** rather than serialized cross-scene references — keeps the Player's components decoupled from the manager's placement in the hierarchy. (Originally `FindFirstObjectByType`; switched to `FindAnyObjectByType` in Iteration 3 since the former is obsolete and ordering guarantees aren't needed for a one-of-each singleton lookup.)
- **`AttentionManager.AttendingTarget`** exposes the in-progress attend target separately from `CurrentTarget` (the detector's nearest target, which can change mid-hold). `AttendableOverlayUI` reads `AttendingTarget` while `State == Attending` and `CurrentTarget` otherwise, so the overlay always reflects the attendable actually being held.
- **`AttendableOverlayUI` builds its `VisualElement` tree entirely in code** (`OnEnable`, via `UIDocument.rootVisualElement`) and creates a runtime `PanelSettings` via `ScriptableObject.CreateInstance` rather than referencing UXML/USS/PanelSettings assets — keeps the overlay code-first/grey-box and avoids asset-reference wiring through MCP-Unity's `update_component` (which has no reliable way to assign `UnityEngine.Object` asset references). The hold-progress bar reuses P1's `[####....]` text-bar pattern (10 segments, built from `HoldProgress01`).
- **"Overlay UI" GameObject** (root, `UIDocument` + `AttendableOverlayUI`) is a new sibling to `AttentionManager` in the scene — UI reads `AttentionManager.Instance` state each frame in `Update()`, consistent with "UI holds no game logic."
- **`PlaceholderAttendable` removed in Iteration 4**: now that `GenericWildernessSpotAttendable` is a real attendable type, the script, its "Placeholder Attendable" GameObject, and the corresponding scene wiring were all deleted per the removal note this section previously carried.
- **`GenericWildernessSpotAttendable` (Iteration 4) lives in `Mossmark.World`**, not `Mossmark.Attention` or `Mossmark.Development` — it's the first of the "generic spots/POIs" attendable types `Mossmark.World` was reserved for, keeping `Mossmark.Attention` limited to the framework itself (interfaces, zones, detector, manager, overlay).
- **`GenericWildernessSpotAttendable.AttentionDuration` is `0f`**: foraging is an instant yield-roll rather than a timed hold. `AttentionManager`'s existing hold-duration logic handles a zero duration as "complete immediately on attend", so no changes to `AttentionManager`/`AttendableOverlayUI` were needed — the overlay's progress bar simply never has time to render. (Superseded in Iteration 8.5 — see below: `AttentionDuration` is now a non-zero tick interval and foraging repeats while held.)
- **Yield rolls use a weighted pool (`ItemYield[] commonYields`) plus an independent rare-drop roll (`ItemYield rareYield` + `rareDropChance`)**: every attend rolls exactly one common yield by weight, and separately has a flat chance (default 8%) to also award the rare yield. Both rolls are logged via `Debug.Log` for now — there's no toast/notification UI yet.
- **`ItemDatabase` (from PROTOTYPE2.md's P1 reference list) was deferred**: `GenericWildernessSpotAttendable` references `ItemDefinition` assets directly via serialized `ItemYield` fields, and `InventoryManager` only needs `ItemDefinition` identity (not id-based lookup). Revisit if a system needs to resolve items by string id (e.g. save/load, crafting recipes).
- **`InventoryManager.AddItem` returns the number of units actually added**; any remainder when both the matching stacks are full and `CarryLimit` (8 stacks) is reached is refused outright rather than dropped on the ground or queued — callers (e.g. `GenericWildernessSpotAttendable`) log a "no room to carry it" message when `added == 0`.
- **`InventoryUI` reads `InventoryManager.Instance` in `Start()`, subscribing to `InventoryChanged` there** rather than `OnEnable()` — same cross-GameObject `Awake`-ordering reasoning as `AttentionManager`'s `Start()`-time lookups, since `Start()` is guaranteed to run after every `Awake()`.
- **"Inventory UI" GameObject** (`UIDocument` + `InventoryUI`) is another code-first overlay sibling to "Overlay UI", anchored top-left vs. the attendable overlay's placement — both follow the same runtime-`PanelSettings`/`VisualElement`-tree-in-code pattern established in Iteration 3.
- **New `Mossmark.Day` namespace (Iteration 5)** holds the daylight/day-clock system — `DayCycleManager`, `DayCycleAmbientTextData`, `DayCycleUI`. Kept separate from `Mossmark.Attention` even though attention spends daylight: "attention is the clock" per PROTOTYPE2.md, but the day-cycle state itself is a concern other future attendables (buildings, NPCs, the Iteration 6 bedroll) will also read/write without being part of the attention framework.
- **`IAttendable.RequiresDaylight`** is the new interface member deciding whether `AttentionManager` calls `DayCycleManager.SpendDaylight()` after `OnAttentionComplete()`, and whether `AttendableOverlayUI` replaces the attendable's normal interaction line with "Too late to start that now." when `DayCycleManager.HasDaylight` is false. `AttentionManager.HandleHoldStarted` also checks this *before* starting a hold, so a zero-daylight attempt on a daylight-costing target consumes nothing and never enters `Attending`. `GenericWildernessSpotAttendable.RequiresDaylight => true`; non-daylight attendables (e.g. the Iteration 6 bedroll) will return `false`.
- **`DayCycleManager.SpendDaylight()` costs a flat 1 per completed daylight-costing attention** — the direct successor to P1's `ActionsPerDay`/`ActionsRemaining` action-pool. `MaxDaylight` defaults to 24, split into 6 even `DayPhase` bands (Dawn → Morning → Midday → Afternoon → Evening → Dusk, 4 daylight each) via a ratio-threshold `GetPhase()`, so phase-crossing ambient text fires 5 times over a full day.
- **`DayCycleAmbientTextData` is a `ScriptableObject`** (`Assets/Game/Data/Day/DefaultAmbientText.asset`) holding one or more candidate lines per `DayPhase`; `DayCycleManager` picks one at random on each phase crossing and logs it via `Debug.Log`, following Iteration 4's "no toast/notification UI yet" precedent rather than adding a transient-message UI.
- **Wiring `DayCycleManager.ambientTextData`** (a `ScriptableObject` reference, unlike the code-first UI components) couldn't be set via MCP-Unity's `update_component` per the asset-reference limitation noted above — the GameObject/component were added via MCP, then the scene `.unity` file was hand-edited to add `{fileID: 11400000, guid: ..., type: 2}` for the `DefaultAmbientText` asset, followed by `load_scene` to pick up the change before re-saving.
- **"Day Cycle UI" GameObject** (`UIDocument` + `DayCycleUI`) is another code-first overlay sibling, anchored top-right with a phase label and a `[####....]`-style daylight bar (20 segments, same text-bar pattern as the hold-progress bar) that dims to translucent white at zero daylight. (Superseded — see below: the daylight bar was replaced by an ambient-text label.)
- **`DayCycleManager.Rest()` (Iteration 6)** is a coroutine-driven fade-to-black / reset / fade-in: `RestRoutine()` sets `IsTransitioning = true`, fades `FadeAmount01` 0→1 over `transitionFadeDuration` (0.6s), resets `DaylightRemaining` to `maxDaylight` and calls `SetPhase(DayPhase.Dawn)`, fades 1→0, then clears `IsTransitioning`. `SpendDaylight`'s inline phase-change/ambient-text logic was extracted into the shared `SetPhase()` helper so `Rest()` reuses the same ambient-text-on-phase-change behavior.
- **`DayCycleManager.IsTransitioning` is the cross-system lock for the rest transition**: `PlayerController.HandleInput()` zeroes `movement` and returns early while it's true, and `AttentionManager.HandleHoldStarted()` refuses to start a new hold — generalizing P1's `RestInteractable` lock to the whole game loop (movement + attention) rather than just the bedroll itself.
- **`BedrollAttendable` lives in `Mossmark.Day`**, not `Mossmark.World` or `Mossmark.Attention` — it's tightly coupled to `DayCycleManager.Rest()` rather than being a generic world/wilderness attendable. It sets `RequiresDaylight => false` (resting restores daylight, so the "too late to start that now" zero-daylight gate doesn't apply) and `CanAttend() => true`, since `AttentionManager.HandleHoldStarted`'s `IsTransitioning` check already covers re-attending the bedroll mid-transition.
- **`DayTransitionFadeUI`** is a code-first overlay sibling like the others, but with `UIDocument.sortingOrder = 100` so its full-screen black `VisualElement` renders above Overlay/Inventory/Day Cycle UI. It polls `DayCycleManager.Instance.FadeAmount01` every frame and sets the overlay's `backgroundColor` alpha directly — same "manager owns progress, UI just displays it" pattern as `AttendableOverlayUI`'s hold-progress bar, applied to a continuous fade instead of a discrete bar.
- **"Reseeding wilderness spots" (third clause of the Iteration 6 deliverable) is currently a no-op**: `GenericWildernessSpotAttendable` is stateless and instant-yield ("no state, no wait" per PROTOTYPE2.md), so attending Field/Clay Pit again after `Rest()` already behaves identically to before — there's nothing to reseed yet. This will need a real `OnDayAdvanced`-style hook once Iteration 7 (Tended-Style Spots) introduces per-spot state.
- **"Bedroll" GameObject** (`TriangleSpriteGenerator` colored dark red-brown, `CircleCollider2D` trigger radius 0.5, `AttendableZone` + `BedrollAttendable`) was hand-placed in the scene at world (0, 3, 0), following the same pattern as the Field/Clay Pit wilderness spots. **"Day Transition Fade UI"** (`UIDocument` + `DayTransitionFadeUI`) is a new sibling alongside Overlay UI / Inventory UI / Day Cycle UI.
- **`DayCycleManager.DayAdvanced` event (Iteration 7)** fulfills the `OnDayAdvanced`-style hook flagged as deferred above. It fires once per `Rest()`, in `RestRoutine()` right after `DaylightRemaining`/`CurrentPhase` are reset to Dawn but before the fade back in. `TendedSpotAttendable` subscribes in `Start()` and unsubscribes in `OnDestroy()` to advance its mark → wait → harvest countdown.
- **`TendedSpotAttendable` (Iteration 7) lives in `Mossmark.World`**, alongside `GenericWildernessSpotAttendable` — both are wilderness-spot attendable types; tended spots add an `Unmarked`/`Marked`/`Ready` state machine (ported from P1's `TendedSpotInteractable`) on top of the same `IAttendable` shape.
- **State-driven `CanAttend()`/overlay text**: `Unmarked` → `CanMark()` (gated by the cap; overlay shows "(too many spots already tended)" without consuming a hold when capped), `Marked` → `false` (overlay shows "ready in N rest(s)" countdown, blocked from re-marking), `Ready` → `true` (harvest allowed). `Mark()` and `Harvest()` both run inside a single `OnAttentionComplete()`, switched on `state`.
- **`MaxConcurrentMarked` cap via a `static Dictionary<string, int>` keyed by `displayName`** — generalizes P1's `TendedSpotConfig`-keyed `_markedCounts` (one shared SO per spot type) to P2's per-component-config approach, where each `TendedSpotAttendable` instance carries its own `displayName`/`maxConcurrentMarked` rather than referencing a shared asset. `OnDestroy()` decrements the count if a `Marked` spot is destroyed.
- **`TendedSpotAttendable.RequiresDaylight => false`, for both marking and harvesting**: `AttentionManager.CompleteAttention` reads `RequiresDaylight` *after* `OnAttentionComplete()` runs, by which point a successful `Harvest()` has already reset `state` back to `Unmarked` — a state-dependent answer at that point can't distinguish "just harvested" from "nothing to harvest." Rather than add latching state to work around this read-order, tended spots are framed as free "tending" check-ins (cultivation), distinct from `GenericWildernessSpotAttendable`'s daylight-costing active extraction.
- **Visual state feedback via `SpriteRenderer.color` tinting**, not sprite/material swaps: `TriangleSpriteGenerator` bakes its base color into the sprite texture at `Awake()`, and `TendedSpotAttendable.UpdateVisual()` sets the (default-white, multiplicative) `SpriteRenderer.color` directly — unchanged (white) for Unmarked, dark grey for Marked, warm yellow for Ready.
- **`Assets/Refresh` needed after creating a brand-new component's `.cs` file before MCP-Unity's `update_component` can find it**: `recompile_scripts` compiled `TendedSpotAttendable` with 0 errors, but `update_component` still failed with "Component type not found" because no `.meta` existed yet for the new file — `recompile_scripts` compiles but doesn't trigger an `AssetDatabase` import. `execute_menu_item` with `menuPath: "Assets/Refresh"` triggers the import (the `.meta` then appears), after which `update_component` succeeds.
- **"Bramble Patch" GameObject** (`TriangleSpriteGenerator` colored dark magenta/plum, `CircleCollider2D` trigger radius 0.5, `AttendableZone` + `TendedSpotAttendable`) was hand-placed at world (0, -3, 0) as the first `TendedSpotAttendable` test entity: `displayName: "Bramble Patch"`, `tendVerb: "tend"`, `restsToHarvest: 1`, `maxConcurrentMarked: 2`, `harvestYield` → new `berries.asset` `ItemDefinition` (1-2 Berries, weight 1). The `harvestYield.Item` reference was wired via the established hand-edit-`.unity`-then-`load_scene` pattern, since MCP `update_component` can't set `UnityEngine.Object` references on nested serializable fields.
- **New `Mossmark.Development` namespace (Iteration 8)** holds the generic dependency/response resolver: `IDependencyCondition` and its four implementations (`ItemAvailableCondition`, `EntityStateCondition`, `WorldStateCondition`, `TimeCondition`), `DevelopmentStage`, `DevelopmentTrack`, `DevelopableEntity`, and the static `WorldState` flag registry. This generalizes P1's `Entity`/`UpgradeDependency`/`UpgradePool`/`TownEntity` per PROTOTYPE2.md Section 4.
- **`IDependencyCondition` implementations cover four condition shapes**: `ItemAvailableCondition(ItemDefinition item, int count)` checks `InventoryManager.Instance.GetQuantity(item) >= count` (the new `InventoryManager.GetQuantity()` method); `EntityStateCondition(DevelopableEntity entity, int minStageIndex)` checks another entity's `CurrentStageIndex`; `WorldStateCondition(string flagId, bool expected, string unmetDescription)` checks `WorldState.GetFlag(flagId)`; `TimeCondition(int attentionsRequired)` is always "satisfied" in the sense that it never blocks progress accumulation — it exists purely to make `DevelopmentStage.ProgressCost` meaningful (see `ResolveAttention()` below). Each condition exposes `IsSatisfied()` and a human-readable `Describe()` for the "needs ..." overlay/log text.
- **`DevelopmentStage`** is a plain C# class: `Id`, `DisplayName`, `ProgressCost`, and a `params IDependencyCondition[] dependencies` array. `DevelopmentTrack` wraps a flat ordered list of stages (no branching/tree — "Tried not chosen" per PROTOTYPE2.md: stages apply automatically once eligible, not via player choice).
- **`DevelopableEntity` is an abstract `MonoBehaviour` base class**, not a `ScriptableObject` — `PendingProgress`/`CurrentStageIndex` are per-instance runtime state, so each hand-placed entity (Old Cairn, Watcher's Post) subclasses it directly and constructs its own `DevelopmentTrack` in `Awake()`. **Tracks are built programmatically in code, not via `[SerializeReference]` polymorphic lists** — MCP-Unity's `update_component` has no way to author polymorphic `IDependencyCondition` arrays through the inspector, so each subclass's `Awake()` hardcodes its `DevelopmentTrack(new DevelopmentStage(...))` construction, with only the leaf data (e.g. the `sticks` `ItemDefinition`, the `oldCairn` cross-reference) as serialized fields.
- **`ResolveAttention()`'s 3-step order resolves the `TimeCondition` chicken-and-egg problem**: (1) `AddProgress(1)` unconditionally increments `PendingProgress`, regardless of whether the current stage's dependencies are satisfied; (2) `TryApplyStage()` then checks `PendingProgress >= stage.ProgressCost` AND all `dependencies` satisfied — if both hold, it applies the stage (`CurrentStageIndex++`, `PendingProgress -= ProgressCost`, fires `OnDeveloped`, sets `LastAttentionConsumedDaylight = true`); (3) if the stage didn't apply, `LastAttentionConsumedDaylight = false` and `LogDependencyReport()`/`GetNeedsOrDefault()` report which conditions are still unsatisfied. This lets a `TimeCondition(2)` stage accumulate its 2 attentions of progress even while another condition (e.g. `EntityStateCondition`) remains unsatisfied — progress and dependency-gating are decoupled, so neither condition type starves the other.
- **`RequiresDaylight => LastAttentionConsumedDaylight` (the "read-after-`OnAttentionComplete`" pattern, first established by `TendedSpotAttendable` in Iteration 7)**: `AttentionManager.CompleteAttention` calls `OnAttentionComplete()` (which runs `ResolveAttention()` and sets `LastAttentionConsumedDaylight`) *before* reading `RequiresDaylight` — so a blocked/no-progress-applied attend (dependencies unsatisfied) consumes no daylight, while a stage-applying attend does. Both `OldCairnAttendable` and `WatchersPostAttendable` use this directly rather than tracking their own latch.
- **`InventoryManager.GetQuantity(ItemDefinition item)`** returns the total carried count across all stacks of that item — added to back `ItemAvailableCondition` without exposing the internal stack list.
- **`WorldState`** is a static class wrapping a `Dictionary<string, bool>` flag registry (`GetFlag`/`SetFlag`), scoped process-wide for this prototype (no save/load yet) — the simplest possible backing for `WorldStateCondition`'s "a flag set elsewhere" case.
- **Three hand-placed Iteration 8 test entities**, all originally `AttentionDuration => 0f` (instant, like the other generic spots; superseded — see below, each now holds for 2 seconds):
  - **"Old Cairn"** (`Mossmark.Development.OldCairnAttendable`, grey `{0.5,0.5,0.5,1}`, world (3, 3)) — one stage "Repair the Cairn" (`progressCost: 1`) gated by `ItemAvailableCondition(sticks, 2)` + `WorldStateCondition("signal_lit", true, "needs the signal fire lit nearby")`.
  - **"Signal Fire"** (`Mossmark.World.SignalFireAttendable`, orange `{0.9,0.45,0.1,1}`, world (3, -3)) — a one-shot toggle with no `DevelopableEntity`/track at all; `OnAttentionComplete()` calls `WorldState.SetFlag("signal_lit", true)` once `CanAttend()` (i.e. `!GetFlag`) is true, demonstrating the "flag set elsewhere" half of `WorldStateCondition`.
  - **"Watcher's Post"** (`Mossmark.Development.WatchersPostAttendable`, slate blue `{0.35,0.45,0.6,1}`, world (-3, -3)) — one stage "Take Up the Watch" (`progressCost: 2`) gated by `EntityStateCondition(oldCairn, 0)` + `TimeCondition(2)`, cross-referencing the Old Cairn instance directly.
  - All three follow the established 7-fileID GameObject pattern (custom component + `AttendableZone` + `CircleCollider2D` trigger radius 0.5 + `TriangleSpriteGenerator` + `SpriteRenderer` + `Transform`), wired via the hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern for the `sticks` and `oldCairn` object references.
- **Deferred from Iteration 8**: a `TownEntity`/town-scope registry for conditions that need town-wide state (vs. per-entity/world-flag state); `VisualColor`/yield-pool fields on `DevelopmentStage` for stage-driven visual changes (Iteration 7's `TendedSpotAttendable.UpdateVisual()` remains the only tinting precedent). Revisit when a later iteration needs town-level aggregation or stage-driven art changes.
- **`IAttendable.ContinueAttending` (Iteration 8.5)** is the new interface member, read *after* `OnAttentionComplete()` like `RequiresDaylight` — true means the hold resets and starts another tick, false means the hold ends here regardless of whether the attend key is still held. Every existing one-shot attendable (`TendedSpotAttendable`, `BedrollAttendable`, `SignalFireAttendable`, `OldCairnAttendable`, `WatchersPostAttendable`) returns a hardcoded `false`, preserving their single-check-in behavior unchanged.
- **`AttentionManager`'s `Attending` state is now a loop (Iteration 8.5)**: a new `isHeld` field tracks the raw Attend-button state from `AttentionInput`'s `HoldStarted`/`HoldReleased` (set at the top of each handler, before any early returns, so it reflects the literal press/release regardless of whether a hold is active). `CompleteAttention()` calls `OnAttentionComplete()`, spends daylight if `RequiresDaylight`, then calls `ShouldContinue()` — which requires `isHeld`, `target.ContinueAttending`, `detector.IsInRange(target)`, and (only if this tick spent daylight) `DayCycleManager.HasDaylight`. If all hold, `holdElapsed`/`HoldProgress01` reset to 0 and the same `attendingTarget` continues into another tick via `UpdateAttending()`; otherwise `FinishAttending()` runs as before. The pre-existing "`AttentionDuration <= 0f` completes immediately on `HandleHoldStarted`" path is unchanged and still only matters for one-shots.
- **`GenericWildernessSpotAttendable` (Iteration 8.5)** gained `[SerializeField, Min(0.1f)] private float tickInterval = 0.5f` backing `AttentionDuration` (previously a hardcoded `0f`). `RollYield()` now returns `bool` — `true` only when the rare-drop roll hits *and* there was room to carry it — and `OnAttentionComplete()` sets `continueAttending = !RollYield()`, so a successful rare drop is the per-tick interrupt that ends the hold (per PROTOTYPE2.md's "Interrupts" rule); a rare drop rolled with no carry room does **not** interrupt, matching its existing "silently skip" logging behavior. The overlay's interaction line changed from "Press E to forage"/"Press E to dig" to "Hold E to forage"/"Hold E to dig" to match the new hold-and-repeat feel. No scene edits were needed — the new serialized field picks up its C# default for the existing Field/Clay Pit instances. (Superseded — see below: `tickInterval` was later replaced by a randomized `minTickInterval`/`maxTickInterval` range.)
- **`DevelopableEntity.LastAttentionConsumedDaylight` split into `LastAttentionMadeProgress` + `LastAttentionAppliedStage` (Iteration 9)**, resolving Section 4's deferred #9/#10 note. `ResolveAttention()`'s ordering is unchanged (`AddProgress()` unconditional, then `TryApplyStage()`, then dependency report if the stage didn't apply) — it now just sets two flags instead of one: `LastAttentionMadeProgress` is true whenever this tick's dependencies were satisfied (progress was productive), regardless of whether a stage applied; `LastAttentionAppliedStage` is true only on the tick that actually crosses the stage threshold. `OldCairnAttendable`/`WatchersPostAttendable` now read `RequiresDaylight => LastAttentionMadeProgress` (their `ContinueAttending => false` from #8.5 is unchanged — they remain one-shot debug entities, so `LastAttentionAppliedStage` doesn't matter to them).
- **`InventoryManager.RemoveItem(ItemDefinition item, int quantity)` (Iteration 9)** mirrors `AddItem`'s "returns units actually affected" shape — it returns how many units were actually removed (capped at what's carried, walking stacks from the end of the list and deleting any that empty out) and only fires `InventoryChanged` if `totalRemoved > 0`. `BuildingAttendable` is the first caller.
- **`BuildingAttendable` (`Mossmark.Development`, Iteration 9) is a generic, reusable `DevelopableEntity` subclass**, unlike Iteration 8's one-off named test entities (`OldCairnAttendable`/`WatchersPostAttendable`) — "Buildings" is a whole taxonomy category that later world-gen (#12) will spawn many instances of with different configs. All building-specific data (`dilapidatedName`, `revivedName`, `repairVerb`, `material`, `materialCostPerTick`, `progressCost`, `tickInterval`, `revivedTint`) is serialized fields; the single-stage `DevelopmentTrack` (`"revive"` → `$"Revive the {revivedName}"`, gated by `ItemAvailableCondition(material, materialCostPerTick)`) is still built in `Awake()` per the no-inspector-polymorphism constraint. `DisplayName` switches dilapidated → revived name once `CurrentStageIndex >= 0`. `RequiresDaylight => LastAttentionMadeProgress`; `ContinueAttending => LastAttentionMadeProgress && !LastAttentionAppliedStage` (the revival tick interrupts the hold). Each productive tick calls `InventoryManager.Instance?.RemoveItem(material, materialCostPerTick)`; a blocked tick calls `LogDependencyReport()` instead and consumes nothing. `OnDeveloped` retints the sprite via `revivedTint` (`UpdateVisual()`), reusing Iteration 7's `SpriteRenderer.color`-tinting approach.
- **`TimeCondition.IsSatisfied()` now always returns `true` (Iteration 10)**, restoring its original Iteration 8 intent ("N attentions of accumulated progress with no other requirement" — never blocks `LastAttentionMadeProgress`): the threshold check belongs to `DevelopmentStage.ProgressCost` via `TryApplyStage()`, not the dependency itself. The prior `PendingProgress >= requiredProgress` form made every pre-threshold tick non-productive (`LastAttentionMadeProgress = false`), which would end a `ContinueAttending`-based hold immediately — incompatible with `NpcAttendable`'s ongoing multi-tick hold (below) while progress accumulates toward the threshold. `requiredProgress` is now an unused constructor parameter, kept purely so call sites self-document the threshold they're pairing with. Verified this doesn't change `WatchersPostAttendable`'s functional outcome — it only changes intermediate debug-log wording for ticks after Old Cairn develops but before `PendingProgress >= 2`.
- **`NpcAttendable` (`Mossmark.Development`, Iteration 10) is the first entity where `TryApplyStage()`'s random-among-available selection acts as an actual specialization draw**: its `DevelopmentTrack` holds three same-cost stages (`forager`/`caretaker`/`tinkerer`, each `progressCost: 4`, each gated by `TimeCondition(progressCost)` — unconditionally satisfied per the change above), so once `PendingProgress` reaches 4 all three are simultaneously eligible and `TryApplyStage()`'s existing random pick chooses one. `RequiresDaylight => LastAttentionMadeProgress` and `ContinueAttending => LastAttentionMadeProgress && !LastAttentionAppliedStage` (same shape as `BuildingAttendable`'s revival tick): ticks 1-3 are productive and continue the hold (spending daylight, logging "progress N."), tick 4 both applies a stage — interrupting the hold — and fires `OnDeveloped`. `HandleDeveloped` sets `specializedName` (`"{genericName} the Forager/Caretaker/Tinkerer"`) and retints the sprite (green/orchid/gold) via `SpriteRenderer.color`, the same Iteration 7/9 tinting approach. `GetOverlayInteractionLine()` returns "Hold E to spend time with {genericName} - they need more time to find their place" pre-specialization — resolving the "needs more time vs. needs diagnosis" open question: "needs more time" is the normal, non-blocking, productive-tick prompt, not a dependency-unmet message (`GetNeedsOrDefault` is effectively a no-op here, since `TimeCondition` never reports unmet needs) — and "{specializedName} has found their place here." post-specialization. `CanAttend() => CurrentStageIndex < 0` makes the NPC inert once specialized; further advancement of a specialized NPC remains deferred/unresolved, per PROTOTYPE2.md.
- **"Wanderer" GameObject (Iteration 10)** (`NpcAttendable` + `AttendableZone` + `CircleCollider2D` trigger radius 0.5 + `TriangleSpriteGenerator` colored cream/tan `{0.85,0.78,0.65,1}` + `SpriteRenderer` + `Transform`) was hand-placed at world (6, 0) — one step further east along the x-axis from "Field" (3, 0), extending the existing layout without collision. Configured `genericName: "Wanderer"`, `progressCost: 4`, `tickInterval: 0.5`. Added via the established 7-fileID GameObject pattern (fileIDs 900500001-900500007) plus a `SceneRoots.m_Roots` entry; unlike Iterations 8/9, no `UnityEngine.Object` reference fields needed wiring, so the hand-edit-then-`load_scene`/`save_scene` pattern was used only for inserting the new block itself, not for any cross-asset references.
- **"Tumbledown Smithy" → "Smithy" GameObject (Iteration 9)** (`BuildingAttendable` + `AttendableZone` + `CircleCollider2D` trigger radius 0.5 + `TriangleSpriteGenerator` colored dark brown-grey `{0.35,0.3,0.28,1}` + `SpriteRenderer` + `Transform`) was hand-placed at world (-3, 3) — the fourth corner completing Iteration 8's three-corner test-entity layout (Old Cairn (3,3), Signal Fire (3,-3), Watcher's Post (-3,-3)). Configured `material: flat_stones` (1 per tick, foraged from the Clay Pit), `materialCostPerTick: 1`, `progressCost: 3`, `tickInterval: 0.5`, `revivedTint: {1, 0.85, 0.5, 1}` (warm gold). The `material` reference was wired via the established hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern. After `save_scene`, `git diff` showed a much larger line count than the hand-edit alone (Unity reordered existing GameObject blocks within the file) — verified benign by confirming every fileID (including the new 900400001-900400007 block and its `SceneRoots.m_Roots` entry) appears exactly once in the saved file.
- **`DeclaredSpecializationNeeds` (`Mossmark.Development`, Iteration 11)** is a static registry — a `HashSet<string>` of specialization ids (`Declare`/`Contains`/`Consume`) — fulfilling Section 7's "Building → NPC demand" shared-needs registry. Specialization ids are just `DevelopmentStage.Id` strings shared between a building's declared demand and an NPC track's stage id (currently only `"smith"`). The new `SpecializationNeededCondition(specializationId)` (`IDependencyCondition`) checks `DeclaredSpecializationNeeds.Contains(specializationId)`.
- **`DevelopableEntity.TryApplyStage()` gained a priority-filter step (Iteration 11)**: after computing `available` (dependencies satisfied + `PendingProgress >= ProgressCost`, as before), if any candidate's `Id` matches a currently-declared need, `available` narrows to just those candidates before the random pick — implementing "if non-empty, draws from it (consuming the need), else draws from the universal track" generically for any `DevelopableEntity` subclass. Whichever stage is chosen is passed to `DeclaredSpecializationNeeds.Consume()` afterward (a no-op for ids that were never declared, so this is safe to call unconditionally).
- **`BuildingAttendable` gained a `declaredSpecialization` field (Iteration 11, default `"smith"`, empty = no demand)**: its `OnDeveloped` handler (renamed from the Iteration 9 inline lambda to `HandleDeveloped`) still retints via `UpdateVisual()`, and additionally calls `DeclaredSpecializationNeeds.Declare(declaredSpecialization)` once revived, logging `"{DisplayName}: the town now needs a {declaredSpecialization}."`.
- **`NpcAttendable`'s `DevelopmentTrack` gained a fourth stage, `"smith"` ("Take up Smithing") (Iteration 11)** — same `progressCost`/`tickInterval` as the universal three, gated by `SpecializationNeededCondition("smith") + TimeCondition(progressCost)`, and deliberately placed **last** in track order. Since `GetNextStage()` (which drives `LastAttentionMadeProgress` and the overlay's "needs more time" line via `GetNeedsOrDefault`) always returns the first not-yet-applied stage — `"forager"` for any unspecialized NPC — appending `"smith"` after the universal three means this new gate affects only which stages are *candidates* at the threshold-crossing tick (tick 4), not ticks 1-3's productivity. At tick 4, if the Smithy has been revived (declaring `"smith"`), `"smith"` is the *only* candidate in `TryApplyStage()`'s narrowed `available` list, so the Wanderer draws `"smith"` deterministically; `HandleDeveloped` adds a `"smith" => ($"{genericName} the Smith", rust/iron tint {0.75,0.4,0.25,1})` case to its switch. If no need has been declared, `"smith"`'s `SpecializationNeededCondition` keeps it out of `GetAvailableStages()` entirely, and the draw is unaffected — identical to Iteration 10's random pick among forager/caretaker/tinkerer. No scene edits were needed for either the Smithy or the Wanderer — both new fields/stages use their C# defaults, per Iteration 8.5's precedent.
- **`PlaceArchetype` and `RegionData` (`Mossmark.World`, Iteration 12)** are new ScriptableObjects under `Assets/Game/Data/World/`. `PlaceArchetype` bundles a wilderness-spot definition (`spotDisplayName`, `spotVerb`, `spotColor`, `commonYields`/`rareYield`/`rareDropChance` — the same `ItemYield` shape `GenericWildernessSpotAttendable` already used) with a specialization definition (`specializationId`, `stageDisplayName`, `npcTitle`, `npcTint`) — one asset is "everything a session draws when it picks this place." `RegionData` is just `regionName` + `archetypePool: PlaceArchetype[]` + `archetypeSelectionCount` (default 3, per PROTOTYPE2.md's "2-3 archetypes"). `TownGenerator`/ground-plane/container concepts from the P1 reference are **not** part of either type — deferred until a later iteration introduces a town container to spawn into.
- **`WorldGenerator` (`Mossmark.World`, Iteration 12) uses `[DefaultExecutionOrder(-1000)]`** so its `Awake()` runs before every other scene object's `Awake()` — critically, before the hand-placed Wanderer's `NpcAttendable.Awake()`, which reads the static `WorldGenerator.SelectedArchetypes` while building its `DevelopmentTrack`. Archetype selection (seeded `UnityEngine.Random.Range` draws without replacement from `regionData.ArchetypePool`, `seed` field or `Environment.TickCount` if `0`) happens in `Awake()` and is logged via `Debug.Log`; actual spawning happens in `Start()` (after all `Awake()`s have run) via two new `Initialize()` methods — `GenericWildernessSpotAttendable.Initialize(displayName, interactionVerb, commonYields, rareYield, rareDropChance, tickInterval)` and `BuildingAttendable.Initialize(dilapidatedName, revivedName, repairVerb, material, materialCostPerTick, progressCost, tickInterval, revivedTint, declaredSpecialization)` — both following the established inactive-GO pattern (`SetActive(false)` → `AddComponent` + `Initialize()` → `AddComponent<AttendableZone>()` → `SetActive(true)`).
- **`NpcAttendable.Awake()` (Iteration 12)** now builds a `Dictionary<string, (string Title, Color Tint)> specializationInfo` (forager/caretaker/tinkerer/smith plus one entry per selected archetype with a non-empty `SpecializationId`) and appends one `DevelopmentStage` per such archetype to the track — same `SpecializationNeededCondition(archetype.SpecializationId) + TimeCondition(progressCost)` shape as Iteration 11's `"smith"` stage, appended *after* it, so `GetNextStage()` still resolves to `"forager"` and ticks 1-3 remain unaffected. `HandleDeveloped` now looks up `stage.Id` in `specializationInfo` (falling back to `specializedName = genericName` if absent) instead of a hardcoded switch, so the same handler covers the universal four plus whichever archetype-derived stages this session added.
- **Building specialization bias**: `WorldGenerator.SpawnBuilding()` spawns one new "Crumbling Shed" → "Workshop" `BuildingAttendable` (`material: sticks`, 1/tick, `progressCost: 2`, `tickInterval: 0.5`, gold `revivedTint {0.8,0.7,0.5,1}`) at world (0, 6), whose `declaredSpecialization` is `SelectedArchetypes[Random.Range(...)].SpecializationId` — i.e. one of *this session's* archetypes, not the hardcoded `"smith"` default. `sticks` was chosen as the material because it's already obtainable from the Field's common yield (40% weight), so the Workshop is revivable without any new foraging setup.
- **`DefaultRegion` (`Assets/Game/Data/World/DefaultRegion.asset`, Iteration 12)** ships a pool of 5 `PlaceArchetype` assets under `Assets/Game/Data/World/Archetypes/` (`archetypeSelectionCount: 3`, so each session selects 3 of 5), inspired by IDEAS.md's "Place Archetypes" examples: **Fen Bog** (dig, → Bog Iron common / Crow Feather rare, → "Bog Keeper"), **Old Road** (search, → Flint common / reuses existing Old Coin as rare, → "Herald"), **Deep Wood Shrine** (gather, → Mistletoe common / Raven's Eye rare, → "Hedge Witch"), **Reed Marsh** (cut, → Reeds common / Duck Egg rare, → "Weaver"), **Old Quarry** (chip, → reuses existing Flat Stones as common / Quartz rare, → "Stonemason"). Eight new `ItemDefinition` assets (`bog_iron`, `crow_feather`, `flint`, `mistletoe`, `ravens_eye`, `reeds`, `duck_egg`, `quartz`) were added under `Assets/Game/Data/Items/`; Old Road and Old Quarry deliberately reuse `old_coin`/`flat_stones` to keep some yields cross-archetype-familiar.
- **New wilderness-spot positions `(-6,3)`, `(-6,0)`, `(-6,-3)` and building position `(0,6)` (Iteration 12)** were chosen to stay clear of the Iteration 1-11 hand-placed layout (roughly x ∈ [-3,6], y ∈ [-3,3]) — one spot per selected archetype, west of the existing Clay Pit/Bramble Patch/Wanderer column, with the Workshop due north of the player start.
- **"World Generator" GameObject (Iteration 12)** (`WorldGenerator` + `Transform`, no `SpriteRenderer`/`Collider`/`AttendableZone` — it's a manager, not an attendable) was added via the hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern, mirroring "Day Cycle Manager"'s GameObject shape (`m_Component` lists `Transform` then the manager `MonoBehaviour`). `regionData` (→ `DefaultRegion.asset`) and `buildingMaterial` (→ `sticks.asset`) are the only object references, set directly in the hand-written YAML since MCP-Unity's `update_component` can't assign `UnityEngine.Object` references; `seed: 0` (session-randomized via `Environment.TickCount`), `colliderRadius`, `wildernessSpotPositions`, and `buildingPosition` were also written explicitly for clarity even though some match their C# field-initializer defaults. Verified post-load via `get_gameobject`: `regionData` resolves to "DefaultRegion" and `buildingMaterial` to "sticks". fileIDs 900600001 (GameObject) / 900600002 (MonoBehaviour) / 900600003 (Transform), added as a new `SceneRoots.m_Roots` entry.
- **No Play-mode automation tool exists in MCP-Unity (Iteration 12)**: `execute_menu_item` with `"Edit/Play"` fails (no such default menu item), and no dedicated play/stop tool was found. Verification for this iteration was therefore via `recompile_scripts` (0 errors/warnings), `get_console_logs` (clean), `get_gameobject` (confirms scene wiring/object-reference resolution), and code review of the full `WorldGenerator`/`NpcAttendable`/`PlaceArchetype`/`RegionData` flow — **not** an in-Play-mode run. The manual playtest sequence in PROTOTYPE2.md Section 8 should be run by hand in the Editor to confirm the session-to-session archetype variation, spawn placement, and Wanderer specialization-draw behavior.
- **`AttentionDuration` changed from `0f` to `2f` (pre-Iteration-13 polish pass) for Old Cairn (`OldCairnAttendable`), Signal Fire (`SignalFireAttendable`), Bramble Patch's mark and harvest (`TendedSpotAttendable`), and Watcher's Post (`WatchersPostAttendable`)** — these five one-shot checks (`ContinueAttending => false` on all of them, unchanged) now require a 2-second hold like the ongoing attendables, rather than completing instantly on press. Overlay interaction lines for all five changed from "Press E to ..." to "Hold E to ..." to match (`TendedSpotAttendable`'s `Marked`-state countdown line is unchanged, since marking isn't itself attendable). No changes were needed to `AttentionManager`/`AttendableOverlayUI` — the hold-progress-bar/`HoldProgress01` machinery already existed for ongoing attendables (Iteration 4 noted it "never has time to render" at `AttentionDuration => 0f`) and now simply renders for these too.
- **`GenericWildernessSpotAttendable`'s `tickInterval` (single field, default `0.5f`, Iteration 8.5) replaced with `minTickInterval`/`maxTickInterval` (defaults `1.5f`/`2f`) (pre-Iteration-13 polish pass)** — `AttentionDuration` now returns a `currentTickInterval` rerolled by a new `RollTickInterval()` (`Random.Range(minTickInterval, maxTickInterval)`), called once in `Awake()` and again at the end of every `OnAttentionComplete()`, so each forage/dig tick takes a freshly-randomized 1.5-2s rather than a fixed 0.5s. `Initialize()`'s signature grew the corresponding `minTickInterval`/`maxTickInterval` parameters; `WorldGenerator.SpawnWildernessSpot()` passes `1.5f, 2f`. The Field/Clay Pit's orphaned `tickInterval: 0.5` scene entries are silently ignored per the established orphaned-key precedent (Iteration 8.5) — both pick up the new C# defaults.
- **`BuildingAttendable`'s `tickInterval` (single field, default `0.5f`, Iteration 9) similarly replaced with `minTickInterval`/`maxTickInterval` (defaults `2f`/`3f`) (pre-Iteration-13 polish pass)**, same `RollTickInterval()`/`currentTickInterval` shape as above — rerolled in `Awake()` and at the end of `OnAttentionComplete()`. `Initialize()`'s signature grew accordingly; `WorldGenerator.SpawnBuilding()` (the Workshop) passes `2f, 3f`. The Smithy's orphaned `tickInterval: 0.5` scene entry is likewise ignored and picks up the new defaults. `NpcAttendable`'s `tickInterval` (still a single `0.5f` field, Iteration 10) was **not** changed — its tuning remains deferred to Iteration 17 per PROTOTYPE2.md's "Ongoing-hold tick interval tuning" open question.
- **`DevelopableEntity.CanMakeProgress()` (pre-Iteration-13 polish pass)** — a new public method (`GetNextStage() != null && stage.AreDependenciesSatisfied(this)`) mirroring the productivity check `ResolveAttention()` already performed internally, now exposed so `CanAttend()` can gate on it *before* entering the `Attending` hold. `OldCairnAttendable.CanAttend()` and `WatchersPostAttendable.CanAttend()` (both previously `true`) now read `CanMakeProgress()`, so approaching either with unmet dependencies (e.g. no Sticks for the Cairn, Old Cairn not yet developed for the Watcher's Post) no longer enters `Attending` at all — the overlay's "needs ..." line still shows via `GetOverlayInteractionLine()`, but holding E does nothing. `BuildingAttendable.CanAttend()` (previously `CurrentStageIndex < 0`) also now reads `CanMakeProgress()`, which produces the same "blocked once revived" result (`GetNextStage()` returns `null` after the single revival stage applies) while additionally blocking a pre-revival hold when the required material isn't carried. `NpcAttendable.CanAttend()` (`CurrentStageIndex < 0`) was deliberately left unchanged — its "draws once, becomes inert" semantics don't map onto `CanMakeProgress()` the same way, since an unspecialized NPC's `TimeCondition`-gated stages are always "satisfied" by design (per Iteration 10's `TimeCondition` fix).
- **"stamina" renamed to "daylight" project-wide (pre-Iteration-13 polish pass)**: every C# identifier, comment, and the scene's serialized field were mechanically renamed (`StaminaRemaining`→`DaylightRemaining`, `MaxStamina`→`MaxDaylight`, `HasStamina`→`HasDaylight`, `SpendStamina()`→`SpendDaylight()`, `StaminaChanged`→`DaylightChanged`, `RequiresStamina`→`RequiresDaylight`, `GetPhase(int stamina)`→`GetPhase(int daylight)`, `maxStamina: 24`→`maxDaylight: 24` in `Overworld.unity`), plus IDEAS.md/PROTOTYPE2.md/this file. "Daylight" reads more naturally as the resource the day-clock tracks; no behavioral change.
- **`DayCycleUI`'s `[####....]`-style daylight bar replaced with an ambient-text label (pre-Iteration-13 polish pass)**: `DayCycleManager` gained `public event Action<string> AmbientTextChanged`, fired from `SetPhase()` alongside the existing `Debug.Log($"[{CurrentPhase}] {ambientText}")` (the log is kept). `DayCycleUI` dropped `BarSegments`/`staminaLabel`/`Refresh()`/`HandleStaminaChanged`/the `DaylightChanged` subscription entirely, replacing them with `ambientTextLabel` (12px, `WhiteSpace.Normal`, `TextAnchor.UpperRight`, `container.maxWidth = 260`) subscribed to `AmbientTextChanged` in `Start()`. The label sits below `phaseLabel` in the same top-right container and starts empty until the first phase crossing fires the event — surfacing `DefaultAmbientText.asset`'s per-phase lines (e.g. "The morning is still young.") in the HUD instead of only the console.
- **`RealizedSpecializations` (`Mossmark.Development`, Iteration 13)** is the mirror image of Iteration 11's `DeclaredSpecializationNeeds`: a static `HashSet<string>` of specialization ids that *have been realized* by some NPC (vs. "needed" by some building), with `Declare`/`Contains`. The new `SpecializationRealizedCondition(specializationId, needsDescription)` (`IDependencyCondition`) checks `RealizedSpecializations.Contains(specializationId)`. `NpcAttendable.HandleDeveloped` calls `RealizedSpecializations.Declare(stage.Id)` whenever `specializationInfo.TryGetValue` succeeds — i.e. for every specialization stage, universal (forager/caretaker/tinkerer/smith) or archetype-derived alike, with no special-casing.
- **`ItemYieldRoller` (`Mossmark.World`, Iteration 13)** extracts `GenericWildernessSpotAttendable`'s yield-roll logic (weighted common-pool pick + an independent rare-drop roll, returning `true` only on a successful rare drop with carry room) into a static helper `Roll(string sourceName, string foundVerb, ItemYield[] commonYields, ItemYield rareYield, float rareDropChance)`, shared by `GenericWildernessSpotAttendable` (`foundVerb: "foraged"`, no behavior change) and the new `PoiAttendable` (`foundVerb: "found"`).
- **`PoiAttendable` (`Mossmark.World`, Iteration 13) is a new `IAttendable`, deliberately not a `DevelopableEntity`** — a POI has nothing to develop, only to unlock, per PROTOTYPE2.md Section 6. Mechanically it's an ongoing wilderness spot (same `minTickInterval`/`maxTickInterval`/`ContinueAttending`-interrupt shape as `GenericWildernessSpotAttendable`, via `ItemYieldRoller`), plus an optional `IDependencyCondition gate`. `CanAttend() => gate == null || gate.IsSatisfied(null)`, and `GetOverlayInteractionLine()`'s locked branch calls `gate.GetNeedsDescription(null)` — passing `null` for the `DevelopableEntity entity` parameter is safe because none of the existing `IDependencyCondition` implementations (`ItemAvailableCondition`, `EntityStateCondition`, `WorldStateCondition`, `TimeCondition`, `SpecializationNeededCondition`, and now `SpecializationRealizedCondition`) actually read it.
- **`PlaceArchetype` gained a `[Header("Point of Interest")]` block (Iteration 13)**: `poiDisplayName`, `poiLockedDescription`, `poiVerb` (default `"search"`), `poiColor`, `poiCommonYields`/`poiRareYield`/`poiRareDropChance` (default `0.05f`, vs. the wilderness-spot default `0.08f`) — the same `ItemYield[]`/`ItemYield` shape as the existing wilderness-spot fields, bundling "everything a session draws when this archetype's POI unlocks" alongside the existing spot/specialization data.
- **`WorldGenerator.SpawnPois()` (Iteration 13)** spawns one `PoiAttendable` per selected archetype at a new `poiPositions` array (`(-9,3), (-9,0), (-9,-3)`) — one column further west than Iteration 12's wilderness spots (`x = -6`), keeping clear of the full Iteration 1-12 layout (`x` in `[-9, 6]`, `y` in `[-3, 6]`). Each POI's gate is built inline as `new SpecializationRealizedCondition(archetype.SpecializationId, $"needs a {archetype.NpcTitle} in town")` — the POI unlocks once *any* NPC realizes that archetype's specialization (e.g. Fen Bog Hollow opens once some NPC becomes "... the Bog Keeper"). Follows the same inactive-GO `Initialize()` pattern as `SpawnWildernessSpot`/`SpawnBuilding`.
- **Five archetypes' POI data (Iteration 13)**, each pairing the archetype's *former rare wilderness yield* as the POI's new common yield with a brand-new POI-exclusive rare item (`poiRareDropChance: 0.05`): **Fen Bog Hollow** (dredge; common Crow Feather, rare new `sunken_bell` "Sunken Bell"), **Old Road Checkpoint** (search the ruins; common Old Coin, rare new `tally_stick` "Tally Stick"), **Old Quarry Cut** (dig through the rubble; common Quartz, rare new `carved_sigil_stone` "Carved Sigil Stone"), **Reed Marsh Blind** (search the blind; common Duck Egg, rare new `woven_reed_charm` "Woven Reed Charm"), **Deep Wood Shrine - Inner Grove** (approach the inner grove; common Raven's Eye, rare new `antler_talisman` "Antler Talisman"). The five new `ItemDefinition` assets live under `Assets/Game/Data/Items/`, following the established `.asset`/`.asset.meta` YAML template.
- **Hand-editing `PlaceArchetype` `.asset` files for new serialized fields (Iteration 13) requires matching the existing 2-space indent level of `MonoBehaviour`'s other fields** (and 4-space for `ItemYield` sub-fields, with the array dash at the 2-space level) — a first attempt that appended the new `poi*` fields at column 0 parsed as siblings of `MonoBehaviour` itself rather than fields of it, producing `"Failed to parse at \"- Item: \""` console errors on `Assets/Refresh` until re-indented to match `commonYields`/`rareYield`'s existing layout.
- **`InventoryManager.ClearInventory()` (Iteration 14)** mirrors `RemoveItem`'s "returns units affected" shape but wipes every stack at once (`stacks.Clear()`, returns the total quantity that was carried, fires `InventoryChanged` only if that total is > 0) — the backing for Wandering Things' "lose everything" bad outcome, which is a full pack-loss rather than a per-item removal.
- **`WanderingThingAttendable` (`Mossmark.World`, Iteration 14) is a new `IAttendable`, deliberately not a `DevelopableEntity`** — per PROTOTYPE2.md's taxonomy, a wandering thing has nothing to develop, only a single attention that resolves it into a good or bad outcome. `AttentionDuration => 2f`, `RequiresDaylight => true` (the encounter spends the day's time regardless of outcome), `ContinueAttending => false` (one check-in fully resolves it). A private `Update()` counts down `lifespanRemaining` (seeded from `lifespanSeconds` in `Awake()`) and self-despawns via the same `Despawn()` path as a resolved attend if it reaches zero before being attended — "available for a time, then disappears" per PROTOTYPE2.md, with no roaming/pathing.
- **Good/bad resolution in `OnAttentionComplete()` (Iteration 14)**: rolls `UnityEngine.Random.value < goodChance` where `goodChance = baseGoodChance` (+ `goodChanceBonus` if `favorableSpecializationId` is non-empty and `RealizedSpecializations.Contains(favorableSpecializationId)`) — the "town development shifts the odds" hook from PROTOTYPE2.md, reusing Iteration 13's `RealizedSpecializations` registry rather than adding a new one. Good rolls `ItemYieldRoller.Roll(displayName, "received", goodYields, null, 0f)` (no rare-drop roll — `goodYields` is itself the "special drop pool"); bad rolls call `InventoryManager.Instance?.ClearInventory()` and, if `badDaylightCost > 0`, an extra `DayCycleManager.Instance?.SpendDaylight(badDaylightCost)` on top of the attend's own daylight spend. Both branches log a flavor line (`goodFlavor`/`badFlavor`) via `Debug.Log`, then `Despawn()`.
- **`WanderingThingSpawner` (`Mossmark.World`, Iteration 14) keeps exactly one `WanderingThingAttendable` alive at a time** via a `SpawnLoop()` coroutine: wait a random `minSpawnDelay`-`maxSpawnDelay`, `Spawn()` at a random `spawnPositions` entry with a random `minLifespan`-`maxLifespan`, then wait until the spawned instance's `onGone` callback (passed into `Initialize()`) clears `current` before looping again. `Spawn()` follows the established inactive-GO pattern (`SetActive(false)` → `SpriteRenderer` + `TriangleSpriteGenerator.Initialize(color)` + `CircleCollider2D` + `WanderingThingAttendable.Initialize(...)` + `AttendableZone` → `SetActive(true)`), the same shape as `WorldGenerator`'s `SpawnWildernessSpot`/`SpawnBuilding`/`SpawnPois`.
- **`WanderingThingSpawner.PickFavorableSpecialization()` (Iteration 14)** picks one of `WorldGenerator.SelectedArchetypes` at random in `Start()` (after `WorldGenerator.Awake()`, per its `[DefaultExecutionOrder(-1000)]`) and uses that archetype's `SpecializationId` as `favorableSpecializationId`, logging which `NpcTitle` will make travelers "more trusting" once realized — ties the bad-outcome odds to *this session's* randomized town development rather than a hardcoded specialization.
- **Spawn positions `(-6,-6), (-3,-6), (0,-6), (3,-6), (6,-6)` (Iteration 14)** — a row along the southern edge, clear of the full Iteration 1-13 layout (`x` in `[-9, 6]`, `y` in `[-3, 6]`).
- **"Wandering Thing Spawner" GameObject (Iteration 14)** (`WanderingThingSpawner` + `Transform`, no `SpriteRenderer`/`Collider`/`AttendableZone` — it's a manager, not an attendable, mirroring "World Generator"'s shape) was added via the hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern, fileIDs 900700001 (GameObject) / 900700002 (MonoBehaviour) / 900700003 (Transform), with a new `SceneRoots.m_Roots` entry. `goodYields` references two existing `ItemDefinition` assets directly by GUID (`old_coin`, `berries`), set in the hand-written YAML since MCP-Unity's `update_component` can't assign `UnityEngine.Object` references — verified post-load via `get_gameobject` (component present, all scalar fields correct, no console errors) and a post-save fileID-occurrence check (each of 900700001-900700003 appears exactly once, `900700003` appears twice total across `m_Component` and `SceneRoots.m_Roots`, as expected).
- **`ChestAttendable` (`Mossmark.Inventory`, Iteration 15)** is a thin `IAttendable`, not a `DevelopableEntity` — like `BedrollAttendable`, it has nothing to develop, only a menu to open. `AttentionDuration => 2f` (matches the pre-Iteration-13 one-shot-hold polish pass), `RequiresDaylight => false` (opening the chest is a free menu interaction, not active extraction — same reasoning as `TendedSpotAttendable`'s tending check-ins), `ContinueAttending => false` (one check-in opens the menu; the menu itself owns further deposit/withdraw). It owns its own `List<InventoryStack>` (the same class `InventoryManager` uses) with **no cap on stack count or per-stack quantity**, per the design draft — `Deposit()` always succeeds, and `Withdraw(item, quantity)` mirrors `InventoryManager.RemoveItem`'s "returns units actually removed" shape.
- **`ChestUI` (`Mossmark.Inventory`, Iteration 15) is "the one remaining menu surface"** per PROTOTYPE2.md — a code-first `UIDocument`/runtime-`PanelSettings` overlay (same `Resources.Load<ThemeStyleSheet>("OverlayTheme")` + `LegacyRuntime.ttf` fallback-font pattern as every other overlay), `sortingOrder = 50` (above the default-0 HUD overlays, below `DayTransitionFadeUI`'s 100 so a day-transition fade still covers it if left open). It renders two columns, "Pack" and "Chest," each row `{ItemDefinition.DisplayName} x{Quantity}` (Pack additionally shows `/{StackCap}`), with the selected row/column highlighted via `SpriteRenderer`-style background-color tinting (`Color(1,1,1,0.3)` selected vs. `0.08` unselected — the same tint-don't-swap approach Iteration 7 established for sprites, applied to UI Toolkit elements).
- **`ChestUI` reads the "UI" action map's `Navigate`/`Submit`/`Cancel` directly via `InputSystem.actions.FindActionMap("UI")` + `.Enable()`**, the same raw-`InputAction`-read convention `AttentionInput` established for "Gameplay" — there's no `EventSystem`/`InputSystemUIInputModule` in the scene and this project's UI is deliberately code-first, so going through UI Toolkit's focus/event system would be the odd choice here. `Navigate`'s `Vector2` is polled every frame with 0.5-threshold edge-detection: Y moves the row selection (wrapping via `((i + delta) % count + count) % count`), X switches between Pack/Chest (resetting `selectedIndex` to 0). `Submit` (`WasPerformedThisFrame()`) transfers the selected row; `Cancel` closes the menu.
- **Deposit (Pack → Chest) moves a selected stack in full; Withdraw (Chest → Pack) moves at most one `StackCap`'s worth (Iteration 15)**, directly implementing PROTOTYPE2.md Section 5's "withdrawals respect the player's stack cap, one stack per withdrawal." Deposit: `InventoryManager.RemoveItem(stack.Item, stack.Quantity)` (always removes the full amount — it's the player's own stack) then `ChestAttendable.Deposit(item, removed)` (uncapped, never refuses). Withdraw: `Mathf.Min(stack.Quantity, stack.Item.StackCap)` is offered to `InventoryManager.AddItem()`, and only the amount it actually *accepted* (`added` — which can be less if the pack is already full of that item) is removed from the chest via `ChestAttendable.Withdraw(item, added)`. This ordering means nothing is ever lost from the chest if the pack can't accept it, and a chest stack larger than `StackCap` requires repeated Submit-presses to fully drain — exactly the "repeated withdrawals needed for more" rule.
- **Three lock-pattern additions generalize the existing `DayCycleManager.IsTransitioning` lock to `ChestUI.Instance.IsOpen` (Iteration 15)**, each placed immediately after the corresponding `IsTransitioning` check: `AttentionManager.HandleHoldStarted()` refuses to start a new hold while the chest menu is open (so the player can't begin attending something else underneath the menu); `PlayerController.HandleInput()` zeroes `movement` and returns early (so "UI" map's WASD-driven `Navigate` doesn't simultaneously move the player via "Gameplay" map's `Move` — both maps stay enabled at once, which is harmless, but only one should have gameplay *effect* at a time); `AttendableOverlayUI.Update()` sets `overlayRoot.style.display = DisplayStyle.None` and returns (the chest menu covers the screen, so the attendable overlay underneath would be redundant/distracting).
- **"Settlement Chest" GameObject (Iteration 15)** (`TriangleSpriteGenerator` colored warm wood-brown `{0.45,0.32,0.15,1}`, `CircleCollider2D` trigger radius 0.5, `AttendableZone` + `ChestAttendable`) was hand-placed at world (1.5, 3, 0) — immediately east of the Bedroll (0, 3, 0), forming a small "camp" cluster at the player's starting area. **"Chest UI"** (`UIDocument` + `ChestUI`) is a new code-first overlay sibling alongside Overlay UI / Inventory UI / Day Cycle UI / Day Transition Fade UI, at world (0, 0, 0). Both follow the established hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern: "Settlement Chest" reuses the Bedroll's exact 7-component shape/order (fileIDs 900800001-900800007: GameObject, `ChestAttendable`, `AttendableZone`, `CircleCollider2D`, `TriangleSpriteGenerator`, `SpriteRenderer`, `Transform`), "Chest UI" reuses "Inventory UI"'s 4-fileID shape (900900001-900900004: GameObject, `ChestUI`, `UIDocument`, `Transform`). Verified post-load via `get_gameobject` (both GameObjects present with all components/fields correct, no console errors) and a post-save fileID-occurrence count (31 total occurrences across both blocks, matching the expected count from each fileID's role — GameObject fileIDs appear once per referencing component plus their own header, component fileIDs appear twice, and the two root Transforms — 900800007/900900004 — appear a third time in `SceneRoots.m_Roots`).
- **`DeclaredSpecializationNeeds` gained an enumeration accessor, `public static IReadOnlyCollection<string> All => needs` (Iteration 16)** — the registry previously only supported `Declare`/`Contains`/`Consume` (point checks for a single specialization id). The Settlement Horizon's "Town Needs" section needs to list *every* currently-declared, not-yet-consumed need, so `All` exposes the backing `HashSet<string>` read-only rather than adding a parallel list to keep in sync.
- **New `Horizon` action on the `Gameplay` map (Iteration 16)**, a plain `Button` (no `Hold` interaction, unlike `Attend`) bound to keyboard `Tab`. `HorizonUI.Awake()` reads it via the same `InputSystem.actions.FindActionMap("Gameplay")` + `.Enable()` convention as `PlayerController`/`AttentionInput`, and polls `WasPerformedThisFrame()` in `Update()` to toggle the panel — a simple press-to-toggle, unlike `AttentionInput`'s `started`/`canceled` hold tracking, since the Horizon has no hold-duration concept.
- **`HorizonUI` (`Mossmark.Development`, Iteration 16) reframes the originally-planned "next upgrade" Settlement Horizon into a town-wide read of the same "needs" language the per-attendable overlay already shows**, per PROTOTYPE2.md's "Settlement Horizon (Possibility Space)" section — `GetAvailableStages()`/the development track itself is never rendered as a player-selectable choice. It's a singleton (`Instance`/`IsOpen`) code-first `UIDocument`/runtime-`PanelSettings` overlay, `sortingOrder = 50` like `ChestUI` (above the HUD overlays, below `DayTransitionFadeUI`'s 100), using the same `Resources.Load<ThemeStyleSheet>("OverlayTheme")` + `LegacyRuntime.ttf` fallback-font pattern as every other overlay. The panel has two sections built via a shared `BuildSection()` helper: **"The Settlement"** lists one row per `DevelopableEntity` (`DisplayName` + that entity's current `IAttendable.GetOverlayInteractionLine()` — an action prompt, a "needs ..." line via `GetNeedsOrDefault`, or a post-development "realized" message, exactly as the overlay would show it if the player were standing there), and **"Town Needs"** lists `DeclaredSpecializationNeeds.All`, each rendered as `"The town needs a {Humanize(id)}."` (`Humanize()` splits on `_` and title-cases each word, e.g. `"smith"` → `"Smith"`). `Refresh()` re-queries both sections each time the panel opens (not continuously) — the Horizon is a snapshot read like opening a menu, not a live HUD element.
- **Entity enumeration uses `FindObjectsByType<DevelopableEntity>(FindObjectsInactive.Exclude)` (Iteration 16)** — the initial `FindObjectsByType<DevelopableEntity>(FindObjectsSortMode.InstanceID)` call produced 2 CS0618 obsolete warnings (both `FindObjectsByType<T>(FindObjectsSortMode)` overloads are deprecated in this Unity version); switched to the `FindObjectsInactive` overload the warning itself suggested, per the existing `AttentionManager`/`AttendableDetector` precedent for avoiding obsolete `FindObjectsByType`/`FindFirstObjectByType` overloads. Recompile after the fix returned 0 warnings.
- **`HorizonUI` extends the existing `ChestUI`-style lock pattern to a 4th checkpoint plus a mutual-exclusion check (Iteration 16)**: `AttentionManager.HandleHoldStarted()`, `PlayerController.HandleInput()`, and `AttendableOverlayUI.Update()` each gained a `HorizonUI.Instance != null && HorizonUI.Instance.IsOpen` check immediately after their existing `ChestUI.Instance.IsOpen` check (same "covers the screen / owns input while open" reasoning). The reverse direction — preventing the Horizon from opening over the chest — lives in `HorizonUI.Update()` itself: `if (!IsOpen && ChestUI.Instance != null && ChestUI.Instance.IsOpen) return;` refuses to toggle open while the chest menu is open (closing is still allowed unconditionally). Combined with `AttentionManager`'s new check refusing to *open* the chest while the Horizon is open, the two menus can never be simultaneously visible.
- **"Horizon UI" GameObject (Iteration 16)** (`UIDocument` + `HorizonUI`, fileIDs 901000001-901000004: GameObject, `HorizonUI` MonoBehaviour, `UIDocument`, `Transform`) is a new code-first overlay sibling added immediately after "Chest UI" in the hierarchy, reusing "Chest UI"'s/"Inventory UI"'s 4-fileID shape and the established hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern, plus a new `SceneRoots.m_Roots` entry for `901000004`. `HorizonUI.cs`'s `.meta` (GUID `7a265ad4c09a35d41b77a6b3649209a8`) was generated via `Assets/Refresh` before scene wiring, per the Iteration 7 precedent for new component types. Verified post-load via `get_gameobject` (all components present, `IsOpen: false`) and `get_console_logs` (empty), and post-save via a fileID-occurrence count (11 total across 901000001-901000004, matching 4 + 2 + 2 + 3).
- **`SquareSpriteGenerator` (`Mossmark.Visuals`, pre-Iteration-17 polish pass)** mirrors `TriangleSpriteGenerator`'s shape (`[RequireComponent(typeof(SpriteRenderer))]`, `Initialize(Color)`, bakes a 1x1 solid-color sprite at `Awake()`) — a second grey-box visual primitive, used for `WorldLayoutGenerator`'s ground-background squares, which are scaled to arbitrary world-space sizes via `transform.localScale` rather than via texture size.
- **`WorldLayoutGenerator` (`Mossmark.World`, pre-Iteration-17 polish pass)** is a new `[DefaultExecutionOrder(-2000)]` MonoBehaviour — runs before `WorldGenerator`'s `-1000` so its static bounds are populated before any procedural spawning, addressing the "give the world a more organized feel" request by splitting it into a town and a wilderness. `Awake()` spawns two background squares via the inactive-GO pattern + `SquareSpriteGenerator.Initialize()`: "Wilderness Ground" (`wildernessSize` 70x70, light green `{0.55,0.72,0.42,1}`, `sortingOrder -2`) and "Town Ground" (`townSize` 20x20, dark green `{0.16,0.33,0.18,1}`, `sortingOrder -1`), both centered at the world origin (negative sorting orders keep both behind every entity's default-0 sprite). Static `TownBounds`/`WildernessBounds` (`Rect`, centered at origin, derived from the two size fields) plus placeholder `IsInTown(Vector2)`/`IsInWilderness(Vector2)` helpers exist for a future procedural layout system but aren't yet consumed anywhere — per the design request, this pass uses static hand-placed positions, with these members standing in for "placeholder methods and what-have-you" until placement becomes procedural.
- **Town vs. wilderness categorization (pre-Iteration-17 polish pass)**: every hand-placed building/NPC/bedroll/chest already fell within the new 20x20 Town Bounds (x,y ∈ [-10,10]) without needing to move — Player (0,0), Bedroll (0,3), Settlement Chest (1.5,3), Old Cairn (3,3), Signal Fire (3,-3), Watcher's Post (-3,-3), Tumbledown Smithy (-3,3), Wanderer (6,0), and the procedural Workshop/Crumbling Shed (`buildingPosition`, unchanged at (0,6)). Everything else — wilderness spots, POIs, tended spots, and wandering things — was moved out into the 70x70 Wilderness Bounds (x,y ∈ [-35,35]), per "place everything else in the wilderness."
- **Wilderness entity repositioning (pre-Iteration-17 polish pass)**: hand-placed Bramble Patch moved from (0,-3) to (-20,20), Field from (3,0) to (20,20), and Clay Pit from (-3,0) to (0,20) — a row along the town's northern edge, just outside Town Bounds. `WorldGenerator`'s procedural `wildernessSpotPositions` moved from `(-6,3),(-6,0),(-6,-3)` to `(-20,10),(-20,0),(-20,-10)` (a column along the western edge, outside Town Bounds) and `poiPositions` from `(-9,3),(-9,0),(-9,-3)` to `(-28,10),(-28,0),(-28,-10)` (one column further west, near the Wilderness Bounds edge); `buildingPosition` is unchanged at (0,6) since the Workshop/Crumbling Shed is itself a town building. `WanderingThingSpawner`'s `spawnPositions` moved from a row at `y=-6` to `(-20,-25),(-10,-25),(0,-25),(10,-25),(20,-25)` along the wilderness's southern edge.
- **"World Layout Generator" GameObject (pre-Iteration-17 polish pass)** (`WorldLayoutGenerator` + `Transform`, no `SpriteRenderer`/`Collider`/`AttendableZone` — a manager, mirroring "World Generator"'s/"Wandering Thing Spawner"'s shape) was added via the hand-edit-`.unity`-then-`load_scene`/`save_scene` pattern, fileIDs 901100001 (GameObject) / 901100002 (MonoBehaviour) / 901100003 (Transform), with a new `SceneRoots.m_Roots` entry. `WorldLayoutGenerator.cs`'s `.meta` (GUID `1c74dc16179bf74429e63d6c26dae852`) and `SquareSpriteGenerator.cs`'s `.meta` (GUID `8cdd54b5b79e0e64bbf6835965948900`) were generated via `Assets/Refresh` before scene wiring, per the Iteration 7 precedent. The first `load_scene` after this edit hit Unity's "modified externally" reload dialog (it resolved on its own); a follow-up `load_scene` succeeded cleanly. Verified post-load via `get_gameobject` (World Layout Generator, Field, Clay Pit, Bramble Patch, World Generator, and Wandering Thing Spawner all show the expected fields/positions) and `get_console_logs` (no errors/warnings), and post-save via a fileID-occurrence count (901100001 x3, 901100002 x2, 901100003 x3, matching the expected GameObject/MonoBehaviour/Transform reference pattern).
- **`HorizonUI` gated on `WorldLayoutGenerator.IsInTown` (pre-Iteration-17 polish pass)**: now that town/wilderness are spatially differentiated, the Settlement Horizon only makes sense while the player is in town. Rather than a new attendable "kiosk" object (a bigger UX change — Tab-toggle becomes hold-to-open at a fixed location), `HorizonUI.Update()` checks `WorldLayoutGenerator.IsInTown(player.position)` first: outside town bounds, `Tab` is ignored, and if the panel was already open when the player crossed out of town bounds, it force-closes. `HorizonUI.Start()` resolves `player` via `GameObject.FindGameObjectWithTag("Player")` (the tag `PlayerController.Awake()` sets), the same `Start()`-time-lookup convention as `AttentionManager`/`InventoryUI`. This is `IsInTown`'s first real consumer — its doc comment was updated accordingly (no longer "placeholder for future procedural placement").
- **`WorldContext` (Iteration 16.5) is a static facade in `Mossmark.Development`**, not a new manager or ScriptableObject, forwarding `DayCycleManager.Instance.CurrentPhase`, `WorldGenerator.SelectedArchetypes`, `DeclaredSpecializationNeeds.Contains`, `RealizedSpecializations.Contains`, and `WorldState.GetFlag` behind read methods (`CurrentDayPhase`, `SelectedArchetypes`, `IsSpecializationNeeded`, `IsSpecializationRealized`, `GetFlag`). It owns no state — its entire value is that modifiers read from one place rather than each reaching into five different statics directly. Guards `CurrentDayPhase` against a null `DayCycleManager.Instance` (falls back to `DayPhase.Dawn`) since it may be read before the scene is fully initialized.
- **`OutcomeRequest` / `IOutcomeModifier` shape (Iteration 16.5)** is deliberately minimal: `OutcomeRequest` is a plain mutable class with one field (`float ChanceMultiplier = 1f`), and `IOutcomeModifier` has a single `void Apply(OutcomeRequest)` method — the same "one implementation per influence kind" shape as `IDependencyCondition`, but for graduated biasing rather than binary gating. Modifiers are instantiated at the call site (`new RealizedSpecializationChanceModifier(...).Apply(request)`) rather than stored as lists — each call site currently applies exactly one or two modifiers inline, which is simpler than a registry or component collection for this prototype.
- **`RealizedSpecializationChanceModifier(specializationId, 1.6f)` reproduces the prior `goodChanceBonus` behavior exactly** (Iteration 16.5): `baseGoodChance: 0.5 × 1.6 = 0.8`, matching `0.5 + 0.3`. The `goodChanceBonus` serialized field was removed from both `WanderingThingAttendable` and `WanderingThingSpawner`; the `WanderingThingSpawner`'s `goodChanceBonus: 0.3` YAML entry becomes an orphaned key (silently ignored per the established precedent). The `Initialize()` signature of `WanderingThingAttendable` dropped the `goodChanceBonus` parameter.
- **`TwilightChanceModifier(1.5f)` is `GenericWildernessSpotAttendable`'s only modifier (Iteration 16.5)**: `rareDropChance * request.ChanceMultiplier` is passed to `ItemYieldRoller.Roll` — so at Dawn/Dusk the effective rare-drop chance is `rareDropChance × 1.5` (e.g. the Field's default `0.08` becomes `0.12`). POIs share `ItemYieldRoller.Roll` but `PoiAttendable` does not yet apply this modifier — it inherits the base `rareDropChance` unchanged, consistent with POIs being distinctive rare-access locations rather than ambient wilderness foraging spots.
- **Pool-sealing problem and solution (Iteration 17)**: after a specialization stage draws via `TryApplyStage()`, the unchosen pool stages remain absent from `appliedStageIds`, so `GetNextStage()` would return the first unchosen universal stage (e.g. "forager" if "caretaker" drew) rather than the drawn spec's post-spec stages. Fix: `DevelopableEntity` gained `public void MarkStageAsApplied(string stageId)` (adds to `appliedStageIds`, idempotent HashSet add), and `NpcAttendable.HandleDeveloped()` calls it for every pool stage id NOT matching the drawn specialization, plus every other specialization's post-spec stage ids. After sealing, `GetNextStage()` iterates past all sealed stages and reaches the drawn spec's first post-spec stage correctly. `GetNextStage()` itself was changed from `private` to `protected` so `NpcAttendable` can read null (fully developed) for its overlay-line and `CanAttend()` logic.
- **`NpcAttendable` post-spec track architecture (Iteration 17)**: pool stages are built first (forager/caretaker/tinkerer/smith + archetype stages), then `BuildPostSpecStages(archetype)` is called per archetype in a second loop, appending post-spec stages AFTER all pool stages. This preserves the pre-spec invariant (`GetNextStage()` = "forager", every pre-threshold tick is productive, ticks 1–(N−1) never surface a "needs" line). Post-spec stages use `SpecializationRealizedCondition + ItemAvailableCondition` — the SpecializationRealized condition is always satisfied by the time these stages are accessible (the spec was just declared in the same `HandleDeveloped` call that sealed the pool), so the only real gate in gameplay is item availability. Items are NOT consumed when a stage fires (the NPC studies what's in the player's pack, not spends it) — cost is daylight, gate is possession.
- **`CanAttend()` and overlay text after specialization (Iteration 17)**: pre-spec, `CanAttend() => true` (unchanged). Post-spec, `CanAttend() => CanMakeProgress()` — true only when the next post-spec stage's item dependency is met. `GetOverlayInteractionLine()` now has three branches: (1) `drawnSpecializationId == null` → same `GetNeedsOrDefault` prompt as before; (2) `GetNextStage() == null` → "has found their place here." (fully developed, covering universal-spec NPCs with no post-spec stages); (3) else → `GetNeedsOrDefault("Hold E to help {specializedName} develop their craft further")` (shows "needs Nx Item" when item dep is unmet, or the default prompt when met).
- **Hedge Witch + Bog Keeper post-spec stages (Iteration 17)**: each archetype provides two stages using its own `CommonYields[0].Item` and `RareYield.Item` as item dependencies (no new serialized fields on `NpcAttendable`), gated by `SpecializationRealizedCondition + ItemAvailableCondition`. Stage ids double as WorldState flag keys — `HandleDeveloped` calls `WorldState.SetFlag(stage.Id, true)` for any non-pool stage, so the modifier side reads the exact same string. Hedge Witch: "hedge_witch_wound_lore" (progressCost 3, 2x Mistletoe) and "hedge_witch_ravens_eye" (progressCost 4, 2x Raven's Eye). Bog Keeper: "bog_keeper_drainage" (progressCost 3, 3x Bog Iron) and "bog_keeper_iron_sense" (progressCost 4, 2x Crow Feather). Herald/Weaver/Stonemason have no post-spec stages yet; `BuildPostSpecStages` returns empty for them, and sealing in `HandleDeveloped` makes those NPCs inert after specialization as before.
- **`OutcomeRequest.DaylightCostMultiplier` (Iteration 17)** is the second field added to `OutcomeRequest`, extending it per the "add when a specific modifier actually needs one" rule from PROTOTYPE2.md Section 10. `WoundLoreModifier` (new file, `Mossmark.Development`) checks `WorldContext.GetFlag("hedge_witch_wound_lore")` and sets `DaylightCostMultiplier = 0f` — eliminating the extra `badDaylightCost` daylight spend on bad wandering-thing outcomes without touching the base attend cost (`RequiresDaylight => true` still fires). `WanderingThingAttendable.OnAttentionComplete()` now computes `effectiveDaylightCost = Mathf.RoundToInt(badDaylightCost * request.DaylightCostMultiplier)` and only calls `SpendDaylight` if that is > 0.
- **`WorldStateChanceModifier(flagId, multiplier)` (Iteration 17)**: a generic companion to `WoundLoreModifier` for flag-based probability biasing (vs. the flag-based severity biasing `WoundLoreModifier` provides). Now wired for Herald post-spec flags (see Content Pass below). Provides a ready-made type for future chance-shift effects where a post-spec stage sets a flag that `WanderingThingAttendable` reads to bias `goodChance` rather than `badDaylightCost`.
- **Content Pass (post-Iteration-17): deeper development loop** — the prior prototype was too flat (one revival stage per building, one specialization per NPC). Changes across `BuildingAttendable`, `PlaceArchetype`, `WorldGenerator`, `NpcAttendable`, all 5 archetype `.asset` files, and `Overworld.unity`:
  - **`BuildingAttendable` gained an optional Stage 2** (`stage2DisplayName`, `stage2Verb`, `stage2Material`, `stage2MaterialCostPerTick`, `stage2ProgressCost`, `stage2RequiredSpecialization`, `stage2Tint` serialized fields; `InitializeStage2()` for the inactive-GO pattern). `Awake()` builds the track as a `List<DevelopmentStage>`, conditionally appending Stage 2 with `ItemAvailableCondition(stage2Material) + SpecializationRealizedCondition(stage2RequiredSpecialization)` when `stage2DisplayName` is non-empty. `OnAttentionComplete()` captures `bool onStage1 = CurrentStageIndex < 0` *before* calling `ResolveAttention()` so the correct material is consumed after the stage potentially advances. Default `materialCostPerTick` raised to 2 and `progressCost` to 6 (from 1 and 3) to require ~2-3 wilderness trips between stage ticks.
  - **`PlaceArchetype` gained a `[Header("Building")]` block** with all Stage 1 and Stage 2 data except `stage2Material` — `WorldGenerator` reads `PoiCommonYields[0].Item` directly, tying Stage 2's material to the POI's common yield (the former-rare wilderness item). This means Stage 2 is naturally gated behind POI access: the player must realize the archetype's NPC specialization to open the POI, gather the new common POI yield, then fuel Stage 2 with it.
  - **`WorldGenerator` replaces `buildingMaterial`/`buildingPosition` with `buildingPositions: Vector2[]`** (defaulting to `(-5,7), (0,7), (5,7)`). `SpawnBuildings()` loops over all `SelectedArchetypes`, calling `SpawnBuilding(archetype, position)` per archetype; skips if `BuildingDilapidatedName` is empty, and skips Stage 2 initialization if `BuildingStage2DisplayName` is empty or no POI common yield exists. The hardcoded "Workshop/Crumbling Shed" spawn using `sticks` as material was removed; all buildings are now fully driven by archetype data.
  - **`NpcAttendable`: "smith" removed from universal pool** (no hardcoded Smithy building remains to declare it). Universal pool is now forager/caretaker/tinkerer only. `progressCost` default raised from 4 → 8, giving ~2 full game-days before specialization fires (time for the player to revive a building and have it declare the archetype need). **Herald post-spec stages added**: `"herald_trail_markers"` (progressCost 3, 3x Flint = `CommonYields[0]`) and `"herald_toll_records"` (progressCost 4, 2x Old Coin = `RareYield`), each calling `WorldState.SetFlag(stage.Id, true)` on apply. `WanderingThingAttendable.OnAttentionComplete()` applies two `WorldStateChanceModifier` calls for these flags (`1.3f` and `1.2f`) after the existing modifiers, capped with `Mathf.Min(1f, ...)`.
  - **All 5 `PlaceArchetype` assets updated** with full building data (Stage 1 + Stage 2). Materials tie to wilderness common yields; Stage 2 materials are the respective POI common yields. `buildingProgressCost: 6`, `buildingMaterialCostPerTick: 2`, `buildingStage2ProgressCost: 4`, `buildingStage2MaterialCostPerTick: 2` for all archetypes.
  - **Hardcoded Smithy removed from `Overworld.unity`** (fileIDs 900400001-900400007, `{fileID: 900400007}` SceneRoots entry). `Wanderer` `progressCost` updated from 4 → 8. `WorldGenerator` YAML updated: `buildingMaterial`/`buildingPosition` replaced with `buildingPositions` array. **Roughhand NPC added** (fileIDs 901200001-901200007, position (-6,0,0), warm grey `{0.6,0.58,0.54,1}`, `genericName: "Roughhand"`, `progressCost: 8`) — a second hand-placed NPC, following the same 7-component structure as the Wanderer. `{fileID: 901200007}` added to SceneRoots.
  - **`PlaceArchetype.cs` gained `using Mossmark.Inventory;`** — the new Building section references `ItemDefinition`, which is in that namespace.
  - **Intended upgrade arc per session** (3 archetypes selected): Stage 1 revival (6 ticks × 2 material = ~12 wilderness items) → NPC specialization at tick 8 + POI opens → NPC post-spec stage 1 → Stage 2 (4 ticks × 2 POI-yield items) → NPC post-spec stage 2. Across two NPCs and two or three building paths, this yields 4-6 upgrades before "unlocking something more special" (POI access) per the design goal.
- **`WildernessSpotDefinition` ScriptableObject (Iteration 18)** bundles all per-type data for either a generic or tended spot in a single `[CreateAssetMenu]` asset under `Assets/Game/Data/World/Spots/`. The `kind` enum (`Generic`/`Tended`) drives which component `SpawnSpotFromDefinition` adds; fields not relevant to the chosen kind (e.g. `commonYields` on a Tended spot) are ignored at spawn time. All 7 spot types are authored as `.asset` files and referenced as an array (`spotPool`) on the WorldGenerator component in the scene — the same hand-edit-YAML-then-reload wiring pattern used for `regionData`.
- **Rejection-sampling position placement (Iteration 18)**: `WorldGenerator.FindValidPosition()` draws up to 200 candidate points uniformly within `WorldLayoutGenerator.WildernessBounds`, rejects points inside `TownBounds` or within `minSeparation` (2 units) of any already-placed object, and falls back to the closest-so-far candidate after 200 attempts. A single `placedPositions: List<Vector2>` is shared across `SpawnWildernessSpots`, `SpawnPois`, and `SpawnGenericWildernessSpots`, so archetype spots and POIs also participate in the constraint. Building positions remain a fixed `buildingPositions: Vector2[]` array since buildings live in town, not the wilderness — only wilderness objects are randomly placed.
- **Three hand-placed Field/Clay Pit/Bramble Patch GameObjects removed from `Overworld.unity` (Iteration 18)**: fileID blocks 1600088223-1600088229 (Field), 2054293845-2054293851 (Clay Pit), 854711395-854711401 (Bramble Patch), and their SceneRoots entries were removed via PowerShell string-processing. The `wildernessSpotPositions` and `poiPositions` arrays were also removed from the WorldGenerator YAML component entry, replaced with `spotPool` (7 WildernessSpotDefinition refs), `minSpotCount: 10`, `maxSpotCount: 12`, `minSeparation: 2`. These three spot types are now represented as `WildernessSpotDefinition` assets (spot_field, spot_clay_pit, spot_bramble_patch) in the pool and spawned randomly alongside the new types.
- **Seven spot types (Iteration 18)**: Field (forage, sticks/wild greens, rare old coin 8%, light green), Clay Pit (dig, clay/flat stones, rare old coin 8%, ochre), Mushroom Patch (gather, mushrooms 1-3, rare spore cluster 6%, warm brown), Stone Outcrop (chip, pebbles 2-4, rare flint 8%, grey), Hollow Log (search, bark strips 1-3, rare beeswax 8%, dark brown), Bramble Patch (tend, 1 rest, berries 1-2, max 2, plum), Bee Skep (tend, 2 rests, honeycomb 1-2, max 2, golden amber). Six new `ItemDefinition` assets: `mushrooms`, `spore_cluster`, `pebbles`, `bark_strips`, `beeswax`, `honeycomb`.
- **`TendedSpotAttendable.Initialize()` (Iteration 18)** follows the same inactive-GO pattern as `GenericWildernessSpotAttendable.Initialize()`: called before `SetActive(true)`, sets `displayName`/`tendVerb`/`harvestYield`/`restsToHarvest`/`maxConcurrentMarked` so `Awake()` (which runs `UpdateVisual()`) fires with correct data already in place. The static `markedCounts` dictionary remains keyed by `displayName`, so all procedurally-spawned instances of the same spot type still share a cap — if 3 Bee Skeps spawn, they collectively cap at `maxConcurrentMarked: 2`.
- **`WanderingThingSpawner` random placement (post-Iteration-18 polish)**: the hardcoded `spawnPositions: Vector2[]` field was removed; `Spawn()` now calls `FindSpawnPosition()`, which rejection-samples uniformly within `WorldLayoutGenerator.WildernessBounds` (available because `WorldLayoutGenerator` runs at `DefaultExecutionOrder(-2000)`, before this `Start()`), expanding `TownBounds` outward by `minDistFromTown` (default 5) to form an exclusion zone. Falls back to `(wilderness.xMin + minDistFromTown, wilderness.yMin + minDistFromTown)` after 50 failed attempts — always outside the exclusion zone. The orphaned `spawnPositions` YAML in `Overworld.unity` is silently ignored per the established orphaned-key precedent.
- **`WanderingThingAttendable` outcome notifications (post-Iteration-18 polish)**: `OnAttentionComplete()` calls `NotificationManager.Post($"{displayName}: {flavor}")` for both the good and bad outcome branches, using the same text already sent to `Debug.Log` — the existing `NotificationUI`/`NotificationManager` infrastructure (already in the scene at `sortingOrder` default, subscribed via `OnEnable`) handles display for 3 seconds at the bottom-center. The bad-outcome "everything you were carrying is gone." secondary line remains a `Debug.Log` only (it's already implied by the bad flavor text).
- **Rare-drop interrupt intentionally removed from `GenericWildernessSpotAttendable` and `PoiAttendable`**: CLAUDE.md iteration 8.5 described `continueAttending = !RollYield()` — a successful rare drop ending the hold so the moment registers. This was deliberately removed once `NotificationUI` landed: the banner already surfaces the rare drop, making the hold-ending interrupt redundant and slightly disruptive. `ItemYieldRoller.Roll()`'s `bool` return value and the `continueAttending` field are both kept intact as mechanisms for future interrupt conditions.
- **`BuildingStageDef` (`Mossmark.Development`, Iteration 21)** is a `[Serializable]` class with public fields `displayName`, `verb`, `material`, `costPerTick`, `progressCost`, `requiredSpecialization`, and `tint` — one entry per stage in a building's development path. `BuildingAttendable` replaces its parallel `revivedName`/`repairVerb`/`material`/`materialCostPerTick`/`progressCost`/`revivedTint` + `stage2*` field groups with `BuildingStageDef[] stages`; `PlaceArchetype` replaces its mirroring `building*` flat fields with `buildingDilapidatedName`, `buildingDilapidatedColor`, and `BuildingStageDef[] buildingStages`. `WorldGenerator.SpawnBuilding()` becomes a one-call pass-through: `building.Initialize(archetype.BuildingDilapidatedName, archetype.BuildingStages, archetype.SpecializationId, 2f, 3f)`. `InitializeStage2()` is deleted.
- **`BuildingAttendable` post-Iteration-21 naming and overlay conventions**: `stages[0].displayName` is the building's revived name (what `DisplayName` returns after revival); subsequent stage `displayName`s are development action names (e.g. "Shore Up the Channels"). Stage 0 overlay: `"Hold E to {stages[0].verb} the {dilapidatedName}"`; stage 1+ overlay: `"Hold E to {stages[nextIndex].displayName}"` (the action name already reads as a full phrase). `DevelopmentStage` IDs are `"stage_0"`, `"stage_1"`, etc.; stage 0's `DisplayName` is `"Revive the {stages[0].displayName}"`; subsequent stages use `stages[i].displayName` directly. `HandleDeveloped` detects revival by checking `CurrentStageIndex == 0` (already incremented by `TryApplyStage()`) rather than a hardcoded `"revive"` id.
- **Stage 2 material now authored directly in assets (Iteration 21)**: each archetype's `buildingStages[1].material` holds the POI common yield item that was previously sourced indirectly from `archetype.PoiCommonYields[0]` in `WorldGenerator`. The indirect lookup and `archetype.BuildingStage2*` YAML fields are gone; adding or changing a building's stage 2 material requires only editing the archetype asset.
- **`NpcPostSpecStageDef` (`Mossmark.Development`, Iteration 22, G3)** is a `[Serializable]` class with fields `stageId`, `displayName`, `progressCost`, `useRareItem` (bool; `false` = `archetype.CommonYields[0].Item`, `true` = `archetype.RareYield.Item`), `itemCount`, `flavorText` (the log suffix — logged as `$"{specializedName}: {flavorText}"`; the NPC name is prepended in code and excluded from the asset string), and `worldStateFlag` (empty = no `WorldState` flag set on apply). `PlaceArchetype` gained `NpcPostSpecStageDef[] npcPostSpecStages` (new `[Header("NPC Post-Spec Stages")]` block, property `NpcPostSpecStages`, default empty array) — inserted before the Building header. `NpcAttendable.BuildPostSpecStages(archetype)` is now a generic loop over this array, resolving each entry's item via `useRareItem` and constructing `DevelopmentStage(stageId, displayName, progressCost, SpecializationRealizedCondition, ItemAvailableCondition)` — the hardcoded `switch (specId)` block is deleted. `NpcAttendable` builds `Dictionary<string, NpcPostSpecStageDef> postSpecStageDefs` in `Awake()` alongside the existing `postSpecStageIdsBySpecId` loop; `HandleDeveloped`'s post-spec branch uses `postSpecStageDefs.TryGetValue(stage.Id, ...)` to get the flavor text and flag from data, falling back to `stage.DisplayName` if the key is absent. `LogPostSpecEffect` deleted. All 5 archetype assets updated: Bog (2 stages), Sacred Grove (2 stages), Old Road (2 stages), Quarry and Reed Marsh (`npcPostSpecStages: []` — empty for now, new stages require only asset edits).
- **Per-spot tick intervals (Iteration 22, G6)**: `WildernessSpotDefinition` gained `float minTickInterval` (default `1.5f`) and `float maxTickInterval` (default `2f`) — existing spot assets pick up C# defaults with no YAML changes needed. `PlaceArchetype` gained `float archetypeSpotMinTickInterval` (default `1.5f`) and `float archetypeSpotMaxTickInterval` (default `2f`) under the Wilderness Spot header, with public accessors `ArchetypeSpotMinTickInterval`/`ArchetypeSpotMaxTickInterval`. All three hardcoded `1.5f, 2f` calls in `WorldGenerator` replaced: `SpawnWildernessSpot` and `SpawnPoi` use `archetype.ArchetypeSpotMinTickInterval / MaxTickInterval`; `SpawnSpotFromDefinition` (generic kind) uses `def.minTickInterval / maxTickInterval`. Building tick intervals (`2f, 3f` in `SpawnBuilding`) are unchanged.
- **`WanderingThingDefinition` (`Mossmark.World`, Iteration 23, G4+G5)** is a `[CreateAssetMenu]` ScriptableObject under `Assets/Game/Data/World/WanderingThings/` holding all per-creature data previously serialized on `WanderingThingSpawner`: `displayName`, `approachDescription`, `attendVerb`, `color`, `colliderRadius`, `goodYields`, `goodFlavor`, `badFlavor`, `badDaylightCost`, `baseGoodChance`. Also holds `WorldStateOddsModifier[] additionalModifiers` (new `[Serializable]` class with `flagId`/`multiplier`) — the Herald-specific `WorldStateChanceModifier("herald_trail_markers", 1.3f)` and `WorldStateChanceModifier("herald_toll_records", 1.2f)` calls that were hardcoded in `WanderingThingAttendable.OnAttentionComplete()` are now entries in this array. `WanderingThingSpawner` replaced all per-creature fields with `WanderingThingDefinition[] pool`; `Spawn()` picks `pool[Random.Range(0, pool.Length)]`. `WanderingThingAttendable.Initialize()` signature collapsed to `(WanderingThingDefinition def, string favorableSpecializationId, float lifespanSeconds, Action onGone)` — component stores `def` as a private non-serialized field. `favorableSpecializationId` remains a separate parameter (session-specific, chosen by `PickFavorableSpecialization()` from selected archetypes); `WoundLoreModifier` and `RealizedSpecializationChanceModifier` remain hardcoded (special logic that can't collapse to a flag lookup). `traveler.asset` (GUID `140d91ba06944db41b4b054cd852d69a`) is the sole pool entry wired in `Overworld.unity`. The orphaned per-creature keys previously in the scene YAML (`displayName`, `approachDescription`, `goodYields`, etc.) were replaced entirely with the new `pool:` block.
- **`WildernessYieldAttendable` (`Mossmark.World`, Iteration 24, G7)** is a new abstract `MonoBehaviour` base class extracting all shared logic from `GenericWildernessSpotAttendable` and `PoiAttendable`: the seven shared serialized fields (`displayName`, `interactionVerb`, `commonYields`, `rareYield`, `rareDropChance`, `minTickInterval`, `maxTickInterval`), `continueAttending`/`currentTickInterval`/`foundVerb` runtime state, `AttentionDuration`/`RequiresDaylight`/`ContinueAttending` property implementations, `OnAttentionComplete()`/`OnAttentionCancelled()` (the tick logic: set `continueAttending = true`, call `ItemYieldRoller.Roll(...)`, reroll tick interval), `RollTickInterval()`, and a protected `InitializeBase()` for the inactive-GO initialize pattern. `CanAttend()`, `GetOverlayDescription()`, and `GetOverlayInteractionLine()` remain abstract (differ between the two subclasses). `protected virtual float GetEffectiveRareChance() => rareDropChance` allows only `GenericWildernessSpotAttendable` to inject `TwilightChanceModifier` — `PoiAttendable` intentionally inherits the unmodified base rate.
- **`GenericWildernessSpotAttendable` and `PoiAttendable` reduced to thin subclasses (Iteration 24, G7)**: each adds only what's unique. `GenericWildernessSpotAttendable` overrides `GetEffectiveRareChance()` with a `TwilightChanceModifier(1.5f)` application; its `Initialize()` delegates to `InitializeBase()`. `PoiAttendable` stores `lockedDescription` and `gate` (`IDependencyCondition`), sets `foundVerb = "found"` in `Initialize()`, and gates `CanAttend()`/overlay text on `gate.IsSatisfied(null)`. The `ItemYield` `[Serializable]` class definition remains in `GenericWildernessSpotAttendable.cs` (same namespace, same file that originally owned it). No behavior changes to either class.
- **`IDependencyCondition` implementations made `[Serializable]` (Iteration 24, G8)**: `ItemAvailableCondition`, `WorldStateCondition`, `EntityStateCondition`, and `TimeCondition` each gained `[Serializable]` + `using System;` + `using UnityEngine;`, and their `private readonly` fields became `[SerializeField] private` — enabling `[SerializeReference]` polymorphic serialization in Unity YAML while keeping their constructors for all existing programmatic call sites (`new ItemAvailableCondition(item, 2)` etc. still compile unchanged). `SpecializationNeededCondition`, `SpecializationRealizedCondition` were left non-serializable (only used programmatically in `NpcAttendable`/`PoiAttendable.Initialize()`).
- **`LandmarkAttendable` (`Mossmark.Development`, Iteration 24, G8)** is a generic `DevelopableEntity` subclass that replaces the Iteration 8 one-off `OldCairnAttendable`/`WatchersPostAttendable` debug entities. All display/behavior data is serialized: `displayName`, `entityColor`, `attendVerb`, `completedDescription`, `progressCost`, `attendDuration`. The key field is `[SerializeReference] private IDependencyCondition[] conditions` — a polymorphic array whose concrete types (`ItemAvailableCondition`, `WorldStateCondition`, `EntityStateCondition`, `TimeCondition`) are `[Serializable]` and authored directly in the Unity YAML `references:` block, with no code change needed to add new landmark types. `Awake()` builds a one-stage `DevelopmentTrack` from these conditions. `CanAttend() => CanMakeProgress()`, `RequiresDaylight => LastAttentionMadeProgress`, `ContinueAttending => false` — same pattern as the entities it replaces. `OldCairnAttendable.cs`/`.meta` and `WatchersPostAttendable.cs`/`.meta` were deleted; both scene objects (fileIDs 900100002, 900300002) now reference `LandmarkAttendable` (GUID `853030447147c2e41aef7cafaf5503ce`).
- **`[SerializeReference]` YAML format in `Overworld.unity` for `LandmarkAttendable.conditions` (Iteration 24, G8)**: the scene YAML uses `conditions: [{rid: N}, ...]` plus a `references:` block at the same indentation level as other MonoBehaviour fields, containing `RefIds:` — each entry has `rid`, `type: {class: ClassName, ns: Namespace, asm: Assembly-CSharp}`, and `data:` with the concrete field values. The Old Cairn's conditions (`ItemAvailableCondition(sticks, 2)` + `WorldStateCondition(signal_lit, true, ...)`) use rids 5100001/5100002; Watcher's Post (`EntityStateCondition(fileID:900100002, 0)` + `TimeCondition(2)`) uses rids 5300001/5300002. The `EntityStateCondition` at 5300001 cross-references fileID 900100002, which is now the `LandmarkAttendable` component on Old Cairn — correctly resolves to a `DevelopableEntity`.
- **`LandmarkAttendable.cs.meta` GUID was regenerated by Unity (Iteration 24, G8)**: a pre-written `.meta` file (GUID `a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8`) was overwritten by Unity's asset importer during recompile; the actual GUID assigned is `853030447147c2e41aef7cafaf5503ce`. The scene YAML was updated to match before Unity loaded the scene. `WildernessYieldAttendable.cs.meta` (GUID `e7c8f9a0b1d2e3f4a5b6c7d8e9f0a1b2`) was preserved — its pre-written GUID held.
- **`TendedSpotAttendable.harvestYield: ItemYield` → `harvestYields: ItemYield[]` (Iteration 24, G9)**: `Harvest()` now picks one item via a new `static PickWeighted(ItemYield[])` helper (same weighted-random logic as `ItemYieldRoller`'s internal pool pick, inlined since tended spots have no rare-drop roll — `ItemYieldRoller.Roll()`'s signature doesn't fit single-pool-no-interrupt). `Initialize()` signature updated to `ItemYield[] harvestYields`. `WildernessSpotDefinition.harvestYield` likewise renamed to `harvestYields: ItemYield[]`. `WorldGenerator.SpawnSpotFromDefinition()` updated to pass `def.harvestYields`. Both tended spot `.asset` files (`spot_bramble_patch.asset`, `spot_bee_skep.asset`) migrated from `harvestYield: {single object}` to `harvestYields: [{array entry}]`. The CLAUDE.md Iteration 18 bullet's reference to `harvestYield` in `TendedSpotAttendable.Initialize()` is now superseded by this change.
